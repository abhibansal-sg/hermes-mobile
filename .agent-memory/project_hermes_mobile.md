---
name: project-hermes-mobile
description: "Hermes Mobile iOS app + shared dashboard backend — ports, token, branch, broadcast patch state"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6b6cabb9-b6d9-4f9e-9125-9451fe1d94bc
---

**=== WHERE THINGS STAND (2026-06-16) — BUILD 41 VALID, RESEARCH-DRIVEN BUILD-OUT SHIPPED ===**
Build 41 (1.0.1) VALID on TestFlight; branch `phase2-upstream-rebase` @ `a1ab20c63` (pushed origin backup mirror).
Came from studying 11 open-source chat apps (`~/chat-research/REPORT.md`, `REPORT-DATA.md`, `BUILDOUT-SUMMARY.md`)
→ `PROPOSAL.md`/`PLAN.md` → build. SHIPPED: A1 per-bubble `Equatable` short-circuit on MessageBubble (settled bubbles
stop re-evaluating; streaming ~150 body-evals/40ms→1) + A2 `withTransaction(animation:nil)` finalize (no turn-end
flash) — the chat-view smoothness win; plugin-side transcript DELTA route (read-only generation guard,
`plugins/hermes-mobile/transcript_sync.py` decide_delta+shape_messages, 14 pytests; route in dashboard/api.py at
`/api/plugins/hermes-mobile/sessions/{id}/messages?after_id&prefix_count&shape`) + iOS delta-aware fetch
(RestClient.messagesDelta + fetchTranscriptDeltaAware) on open+reconnect with SAFE full-fetch fallback; skeleton/light
shaping. ZERO stock-gateway edits (SessionDB read_only). iOS build+full-test GREEN. Linear ABH-154 (+ABH-153 saga).
DELTA ACTIVATES only when the gateway runs the updated plugin (redeploy :9119 or test instance) — else iOS full-fetch
fallback (safe). DEFERRED (need device loop): iOS skeleton client tiering (backend ready), keyboard rewrite (works on
iPhone fleet), Phase-5 offline outbox. DECIDED: keep eager VStack (A1 validated), keep drawer. NEXT: user device
verdict on 41; then optionally redeploy gateway to activate delta + do deferred items device-verified.

**=== WHERE THINGS STAND (2026-06-14) — BUILD 40 VALID, SMOOTHNESS SAGA AT DEVICE-VERDICT ===**
Trunk = `feat/group-collapse-pin`. Active work branch = `phase2-upstream-rebase`
(HEAD `cb96cdc9a` build 40, pushed to origin/backup mirror). Live gateway :9119
carries the Phase-2 rebased plugin gateway + `session.info` model hot-swap +
stable wire-id `_history_to_messages` ordinal stamp.

**THE SMOOTHNESS ARC (builds 34→40), now essentially closed pending the user's
device feel-test of build 40:** the user fought chat-surface jank for days —
scroll jank, session-switch jank, chats opening on a BLANK page (scroll-up-then-
snap), opening mid-conversation, rare freeze. Root causes were a STACK of three
platform-fights, each masking the next:
 - Build 36 = cache-first architecture (paintFromCache before the REST probe;
   killed the 2.5–4s white void = full-transcript network fetch painted as
   nothing).
 - Build 38 = "the boring rebuild" — deleted ~540 lines of custom scroll
   machinery for stock `ScrollView+LazyVStack+.defaultScrollAnchor(.bottom)` +
   per-session `.id` + `.transaction{animation=nil}`.
 - Build 39 = eager VStack over a bounded 150-msg tail window (killed LazyVStack
   HEIGHT-ESTIMATION = the blank/scroll-up-then-snap) + rigid drawer card (scoped
   the settle spring to the card `.offset` alone, continuous corner-radius ramp).
   **USER VERDICT on 39: "loading fine, always at the newest message, perfect."
   The load blocker — the thing actually gating community launch — is SOLVED.**
 - Build 40 = drawer CHOREOGRAPHY (the last polish): (1) open "card snaps right"
   = drag-latch dead-zone applied as a step → FIXED with `dragAnchor` (card tracks
   `dx - dragAnchor`, glued to finger at handoff); (2) close "transcript moves
   before the card" = FIX-4 closed on frame 0 while the async cache paint landed
   mid-slide → FIXED with reveal-on-paint (`SessionStore.open(_:revealOnFirstPaint:)`
   fires the drawer close only after phase-1 first paint; `DrawerView.open` =
   `sessions.open(summary){onNavigate()}`). Files: RootView CompactLayout,
   SessionStore.open + seedTranscriptCacheFirst, DrawerView.open. Full test suite
   GREEN; sim-verified on live gateway/real data (correct chat at newest, ONE
   coherent close spring, no mid-slide reflow per frame-diff, no blank/strand/
   crash). DEVICE-ONLY remaining: the finger-DRAG feel of fix (1) — cliclick
   cannot synthesize a real pan; the user's thumb is the judge. Also for the user
   to judge: keyboard-rise smoothness, short-chat bottom-align acceptance.

**PROCESS RECKONING (this matters for continuity):** 4 prior "verified" builds
failed on the user's device because every gate judged STILLS + unit tests +
SYNTHETIC seeds — and the complaints were about MOTION + REAL chats. New standing
bar (build-39 onward): no ship until the orchestrator personally watches VIDEO of
the exact reported motions, on recordings from the sim, against the user's REAL
sessions (live gateway :9119, token ~/.hermes/dashboard.token, cliclick for mouse
pans on the Simulator window). Stills/agent-claims are insufficient for motion
defects. Harness: /tmp/myverify/. The SWBBuildService wedge recurred mid-build-40
and was cleared by a user logout/login (per [[ops-swbbuildservice-wedge]] — the
window also RELOCATED on relaunch from x=737 to x=1255, silently breaking every
cliclick coord until re-pinned; ALWAYS re-read window origin after a sim relaunch).

**IMMEDIATE NEXT (2026-06-14):** user is concluding this session to COMPACT and
bring a NEW APPROACH to make dev faster (not yet specified — get it at session
start). Per user ruling, after the build-40 device verdict we STOP hand-polishing
the drawer and MOVE ON: triage the broader small-bug list (batch into a couple of
builds) + return to LAUNCH (public repo ~/hermes-mobile-public, copy + assets
~/hermes-demo-video/, PR0 drafted, video footage waits on the now-smooth app).
Smoothness resume detail also in the auto-memory file [[smoothness-round2-resume]].

Native SwiftUI iOS/iPadOS client for hermes-agent, built 2026-06-05 on local branch `hermes-mobile` in ~/.hermes/hermes-agent (upstream NousResearch/hermes-agent — do NOT push).

**TAKEOVER 2026-06-07 (FULL — user ruling):** Claude owns BOTH lanes; dual-executor retired (CLAUDE.md + CODEX-LANE.md amended in repo). UX Gameboard on Linear (parent ABH-59, levels ABH-60..72): L00-05 Done w/ feedback; L06+ pending — user switched to FIX-FIRST level-by-level, live-cockpit mode (user at Mac, direct device installs to iPhone Air `1D51F2FB…`/hw `00008150-000911CA0240401C`, ~2min loop, no TestFlight). Wave UX-1 issues: ABH-73..84 in "Hermes Mobile — Engineering" (P0 bugs ABH-73 delete / ABH-74 mic; batches ABH-79 glass shell→ABH-83 inline approvals; ABH-84 deferred UX-2). LANDED: `6fb76c612` ABH-73 iOS half (close-before-delete + error surface + interrupt rider), `c7743dc9c` ABH-74 voice (gesture re-host, hold-strip stop, 120s watchdog, interruption/disconnect/timeout) — gate GREEN 377/0. Server half (evict-before-delete) lands separately; INERT until 9119 restart (ask user). Subtitle locked: "Your agent, on the go." Archived-chats placement + swipe-anywhere-vs-codeblock questions asked, pending answer. INCIDENTS (both attributed to USER-DRIVEN Codex desktop session while Claude unreachable): 10:54 bulk revert of 27 files to pre-R1 blobs → quarantined `stash@{1}` "QUARANTINE 2026-06-07" (never pop); 12:38 ChatMessagePart ordered-transcript work (+ doc apps/ios/CHAT-THREAD-PARTS-2026-06-07.md) → user ruled adopt-after-adversarial-review (was stash@{0}, integrator applies+drops it on green). Model tiering policy active per [[model-tiering-policy]]; velocity model per [[velocity-model]].

**UX-1 WAVE SHIPPED — TestFlight build 12 VALID (2026-06-08, ASC build 12b274ee, v1.0.1(12)):** consolidated drop of the whole wave since build 11. LANDED on branch hermes-mobile (NOT pushed): full-bleed canvas (NavigationStack-inset root cause, pixel-verified) + native-fallback Liquid Glass edge fade (header-legible) + keyboard-rise + top-gap; drawer feed desktop-parity (min_messages=1, grow-limit, 30-default concurrency-safe fill, heartbeat); transcript REBUILD ABH-87 Batches A-E COMPLETE (one canonical ChatMessagePart model, seed/stream structural-equivalence test, per-cluster collapse, scroll-on-seed-lands-newest + scroll-to-bottom-no-white-void); P0 bug fixes (session delete, voice wedge, manual-entry validation gate); Levels 06-12 fast-pass (L06 files+image preview, L07 settings P0 notif-permission-bridge + QuickCapture REMOVED, L08 panels cron CRUD + skills toggle + usage fix, L11 Spotlight/Handoff receiver + deep-link fallback + App-Intent local-draft). Build 12 commit 1bdef9675. STILL OPEN: L12 a11y sweep (76 hardcoded font sizes, contrast, labels — next), L10 notifications/widgets (device-only verify), parked features ABH-85 swipe actions + ABH-83 inline approvals, user's deferred small-tweaks list. Linear ABH-73..87 mostly done — board needs reconciliation. Delivery: user went OFF local network → TestFlight (devicectl install needs local reachability). Proven TF pipeline: bump CURRENT_PROJECT_VERSION in project.yml → archive (ASC auth flags, cert team 6J4Y9NKRQ2, 3 IOS_APP_STORE profiles valid through 2027-06-05) → PlistBuddy leapfrog gate → exportArchive ExportOptions-TestFlight.plist destination=upload → poll /v1/builds VALID.

**Scope boundary (user, 2026-06-05):** my lane is the app + its immediate backend surface only. Ecosystem ops (gbrain collectors, email auth, cron health) belong to the Hermes agent itself — flag issues, don't fix them.

**UI sprint (in flight 2026-06-05):** drawer navigation (ChatGPT-style, chat-as-home, fresh-chat landing, local-until-first-message sessions), 6 desktop themes (nous adaptive default), QR pairing (`hermes mobile-pair`), push deep-links, hybrid tool rows (live→collapse), agent gutter, mic-in-field composer (tap + hold-to-talk). Batches: A theme engine / B drawer+flow / C chat surface / D polish.

**Wave 2 (locked 2026-06-05, starts after UI sprint):** 2.1 TestFlight+iPad+prod APNs → 2.2 Notifications v2 (actionable approve/deny on push, Live Activity remote updates, per-event prefs) → 2.3 Agent depth (subagent spawn tree, file browser + @-refs; needs 2 token-gated server endpoints) → 2.4 Multi-profile SWITCHER (one active connection, per-profile dashboards server-side; unified inbox deferred). CUT: watch app (actionable push mirrors to watch — covers it), full terminal (cost/benefit). AMENDED: biometric-gated approvals (Face ID for destructive/approve-all) pulled INTO Wave 2.2.

**ASC API (verified working 2026-06-05):** Key ID 3DHXXG4GHQ, Issuer d7deff8e-5489-4d18-995d-c8a10f854118, .p8 at ~/.appstoreconnect/private/AuthKey_3DHXXG4GHQ.p8 (0600). JWT: ES256, aud appstoreconnect-v1.

**TestFlight LIVE (2026-06-06):** app record "Hermes Mobile Gateway" (id 6777140135, bundle ai.hermes.app), current build 1.0.1 (4) = full-native UI + push fix. PIPELINE (proven): cloud signing blocked at App Manager role → MANUAL path: Apple Distribution cert 2L6646GF3C (key+cert in login keychain) + 3 IOS_APP_STORE profiles ("Hermes TF app/share/HermesWidgets", installed in both profile dirs); bump CURRENT_PROJECT_VERSION (project-level in project.yml; Info.plists reference $(CURRENT_PROJECT_VERSION)/$(MARKETING_VERSION) — NEVER static); worktree at HEAD → xcodebuild archive (ASC auth flags) → PlistBuddy-verify CFBundleVersion+ShortVersionString in archive → exportArchive with /tmp/hermes-tf-build/ExportOptions.plist (manual signing map) → poll /v1/builds for VALID. Internal group exists, tester ab0991@gmail.com (account holder; user's NEW Apple ID). GOTCHAS fixed: first upload went as "1.0 (1)" (XcodeGen static plist defaults) — version line leapfrogged with 1.0.1; APNs env detection reads embedded profile's aps-environment VALUE (TF builds carry profiles). UI batches A-I ALL SHIPPED incl. full-native rebuild (system toolbar/List/glass, accent-led themes). User dogfoods via TestFlight; iPhone unreachable via devicectl when away (Tailscale ≠ CoreDevice).

**SESSION HANDOFF (2026-06-06 ~03:30, post-F2 rollout):**
- DONE TONIGHT: UI-G debug bridge LANDED (8e163bbe6 — DEBUG-only SPM pkg, loopback StateServer + store accessors, release purity proven on stripped binary). F1 CLOSED, NOT a P0: 3 live probes (explicit tool prompt / casual phrasing / two-client mirror) all proved tool execution REAL + fully broadcast; probe scripts /tmp/hermes-f1/probe{,2}.py. F2 Notifications v2 LANDED + ROLLED OUT: fde8563d5 (server: POST /api/approvals/respond [REST approve→gateway "once"], aps.category HERMES_APPROVAL/CLARIFY/TURN + hermes.{stored_session_id,destructive,approval_title}, LA registry ~/.hermes/live_activity_tokens.json + notify_live_activity [liveactivity type, topic ai.hermes.app.push-type.liveactivity, ≥3s throttle, end bypasses], PushRegisterBody.events prefs filter) + 673482730 (app: Approve/Deny actions authenticationRequired, per-decision LAContext on destructive [approve-all deliberately in-app-only], LA pushType:.token round-trip, 3 Settings toggles) + 8af1006d2 (contract). Integration gate PASSED (zero bugs, real approval→push→Approve→command executed 644→777; evidence /tmp/hermes-f2-evidence). PROD: dashboard restarted (user pre-authorized), routes verified live, smoke push delivered (200 production). REGISTRY REPAIR: both tokens lacked env (registered against old server) — manual env-probe (NO prune risk) identified 6d1c8762=production (TestFlight phone) / 9d68e94c=sandbox (dev), env fields written back. LESSON: never notify() blind when registry entries lack env — wrong-env send prunes the token.
- ON-DEVICE CHECKLIST for user (UNPROVEN-ON-SIM, needs build 5): lock-screen Approve/Deny action tap; Face ID gate on destructive approval; LA push-token POST + remote LA updates; Settings toggle → re-register POST. Sim can't prove these (no FaceID scripting, SpringBoard banner unreachable, simctl can't deliver liveactivity type).
- IN FLIGHT: TestFlight build 5 (1.0.1) agent running the proven pipeline (bump CURRENT_PROJECT_VERSION→5, worktree archive, PlistBuddy verify, manual-signing export, poll VALID).
- NEXT: F3 mirror-latency — fact base at apps/ios/F3-FACTS.md. TOP HYPOTHESIS H1: CrossClientSync flake = TEST DEFECT (2nd foreign prompt fire-and-forget, no waitForTurnComplete, ran against LIVE 9119; 10/10 fails at exactly the 150s ceiling; broadcast confirmed ON). Decisive: add waitForTurnComplete + run vs own instance. FIX REGARDLESS: server broadcast fan-out is synchronous+sequential, 10s/client blocking write = head-of-line stall of ALL clients (real prod risk). Then F4 = Wave 2.3 (file browser+@refs, subagent tree) / 2.4 (profile switcher). Latency measurements need a QUIET machine (serialize after builds).
- DURABLE ARTIFACTS: batch contracts apps/ios/CONTRACT-*.md (F2 interface pinned in CONTRACT-F2.md); F3-FACTS.md; PR drafts ~/hermes-demo-video/pr-drafts.md (respond endpoint = PR #6 candidate, kept separable); video assets ~/hermes-demo-video/.
- BRANCH: hermes-mobile (~29 commits). NEVER push upstream without user. Working method: contract-first → parallel module fleet → independent adversarial integrator; recon workflows before investigations (F3 recon overturned the premise).
- BLOCKED/PARKED: cso audit (Anthropic verification pending, user will signal); video/X/draft-PR ship-day (user will signal; sequence = finish-polish → demo video → draft PR series + X post same hour → internal TestFlight offer in thread → public after merge). Hardware design review deferred until iPhone CoreDevice-reachable.
- RATIFIED DECISIONS (user, 2026-06-06): (1) OPTION B for upstream multi-client — adopt shared-runtime resume-reuse + transport-rebind guards at PR-series time (full plan apps/ios/UPSTREAM-REVIEW-2026-06-06.md; rebase-lockstep: wrapper drops --tui, ORPHAN_REAP_GRACE=300, iOS teardown recovery); (2) F3-H rollout AUTO on green gate — restart 9119 + TestFlight build 6, no further confirmation; (3) sudo/secret gap rides in F4a; (4) F4a scope = Wave 2.3 core + ALL census extras (branch/checkpoint UX, todo card, tool grouping+detail toggle, sudo/secret). Parity doc: apps/ios/PARITY-2026-06-06.md.
- R1 FULL-SCALE REVIEW WAVE (user-approved, task #9): after automatic sequence, before ship-day — multi-engine: loop-until-dry workflow bug hunt (parallel finder dimensions, 3-lens adversarial verify, 2 dry rounds to exit) + /review skill + /codex cross-model + /cso if unlocked + ios-qa live loop + stock-server re-check; fix loop (P0/P1 immediate w/ adversarial gate, P2 batched, polish backlog task #7 merged); exits with REVIEW-R1 report in apps/ios + clean-bill TestFlight build. R1 report = quality evidence cited in the upstream PR series.
- OVERNIGHT LEDGER (cont.): F4a CLOSED ~07:30 (3-module fleet S/A1/A2 + gate PARTIAL→fix→re-gate GREEN; boundary crack caught: browser/cwd-picker built-but-unmounted, wired in c660a8669+963e35fab; cwd round-trip agent-pwd-proven; suite 236/2; ships as build 7 fb9146b75). F4b builder launched ~07:35 (dormant switcher per CONTRACT-F4B.md). QA-harness learning: coordinate bridge can't see SwiftUI a11y IDs (only XCUITest activates AX); Menu activation flaky under synthesized touch.
- OVERNIGHT LEDGER: F3-H CLOSED ~05:00 (round-1 refuted at gate; round-2 ring-buffer diagnosis → ownership(of:)+localTurnToken fix be12a6b87 → gate 8/8 green, mirror lag 1.1s, budget 15s; 9119 restarted w/ fan-out fix; build 6 VALID). F4a fleet launched ~04:50 (3 modules: S=fs endpoints, A1=composer/@-refs/file browser, A2=subagent tree/sudo-secret/branch UX/todo/tool grouping; contract CONTRACT-F4A.md w/ pinned interfaces).
- OVERNIGHT MANDATE (user, 2026-06-06 ~04:00, user asleep): finish the FULL automatic sequence autonomously — F3-H round 2 (in flight) → auto-rollout (9119 restart + build 6) → F4a (task #8) → F4b (upstream profile model) → Wave 3a security → R1 (task #9). Ship TestFlight builds at batch boundaries (established cadence). House method every batch: contract → fleet → adversarial gate. If a gate refuses twice, diagnose-first round 3; if truly stuck, document + move to next batch and leave a clear morning report. NEVER: push to remotes, start ship-day/PR/X work, touch cso before user signal.

**BUILD 13 SHIPPED — TestFlight VALID (2026-06-08, v1.0.1(13)):** post-build-12 follow-up drop. Commits on hermes-mobile (NOT pushed): `07b5b7666` SCROLL P0 (deterministic open-on-newest + keyboard-follow rebuild — DELETED the DispatchQueue 0.05s timer race; native `.defaultScrollAnchor(.bottom)` + `.scrollPosition(id:.bottom)`; clearance spacer moved ABOVE the bottom anchor so anchor=true content end; new `KeyboardHeightReader` measures real keyboard overlap so the bottom-anchored transcript rises with it; transcript `.ignoresSafeArea(.keyboard)`; UX1PolishTests 16/16); `5537c3c5a` iOS live-sync gap-recovery (broadcast_gap was read from params but the gateway writes it at FRAME TOP LEVEL — added to JSONRPCInboundFrame CodingKeys + threaded into GatewayEvent at the construction site; ConnectionStore REST backfill now fires; ProtocolParityABH46Tests 22/22 — and fixed the false-confidence test that injected it inside params); `7f9413752` keyboard dismisses on drawer-open (root-cause single chokepoint: `.onChange(drawer.isOpen)`→resignFirstResponder, covers swipe/button/⌘F); `9b5306223` build-13 bump. Plus the 7 craft commits + aggressive-foreground-reconnect + settings-first-open-fix already landed since 12. Gates: ASC MAX_BUILD clean (12→13); PlistBuddy passed (app+widget CFBundleVersion=13). Reusable ASC poll helper at /tmp/asc.mjs (key 3DHXXG4GHQ, issuer d7deff8e-5489-4d18-995d-c8a10f854118, app 6777140135). LESSON (self): the issuer was IN THIS MEMORY FILE — check memory before asking the user for credentials.

THREE PRE-COMPACTION WORKFLOWS — ALL RESOLVED:
1. SCROLL/KEYBOARD: the background workflow DIED (transient kill at the session restart/compaction, same mode as the earlier Batch E death) but its work SURVIVED uncommitted in worktree /private/tmp/scrollfix → I rescued it, root-cause-reviewed it (sound, deterministic-by-construction), adopted, verified (build+tests+smoke), shipped as `07b5b7666`. LESSON reaffirmed: long background workflows are fragile across session boundaries — rescue work from the worktree, don't assume it's lost.
2. OFFLINE CACHE P1+P2: GREEN, landed `4ff951404` (dormant; P3 wiring + P4 still DEFERRED to last-before-public-release).
3. BROADCAST DESYNC: root-caused = CLIENT-SIDE, two defects, server path HEALTHY (NO 9119 restart needed). iOS half FIXED (`5537c3c5a`, in build 13). DESKTOP half = desktop drops every foreign (iOS-driven) mirror frame (handleGatewayEvent gates on session_id===activeSessionIdRef; RpcEvent lacks stored_session_id) → bigger than a 5-line fix: a PORT of iOS ownership(of:)+handleForeignFrame adoption (incl. the localTurnInFlight guard) into the desktop apply path. IN FLIGHT as verified workflow **wru9nnymq** (recon→Opus impl→3-lens adversarial verify [correctness/regression/edge]→tsc gate; source-only, user rebuilds desktop). Additive-upstream assessment delivered (full report in transcript wfrzp4kri.output): ~50 lines across 4 files genuinely need upstream seams (the big lever = an `_emit_hooks` subscriber list in tui_gateway/server.py); ~1550 lines can become add-on modules — OPTIONAL cleanup, user decides.

**SCROLL P0 ROUND 2 (build 14, 2026-06-08) — user QA of build 13 found two regressions from the scroll rebuild:** (1) scroll-to-bottom PILL no-op; (2) SHORT chats bottom-aligned with empty upper half. ROOT CAUSES (verified deterministically in sim): (2) plain `.defaultScrollAnchor(.bottom)` sets the `.alignment` role too → bottom-aligns content shorter than the viewport. FIX: `.defaultScrollAnchor(.bottom, for: .initialOffset)` (iOS18) — initial offset only, alignment stays top. VERIFIED in sim (short=top, long=opens-at-bottom). (1) the pill no-op is NOT the `.scrollPosition(id:)` binding (removing it was insufficient) — it is `proxy.scrollTo(bottomAnchor)` failing because `bottomAnchor` is a 1pt LAZY trailing view outside the realized LazyVStack window when scrolled up → scrollTo can't reach it. FIX: iOS18 `ScrollPosition.scrollTo(edge: .bottom)` (scrolls to true content edge, no item id, immune to lazy realization, no-op when content fits). **DETERMINISTIC SIM TEST HARNESS BUILT (DEBUG-only, keep):** `HERMES_UITEST_SEED=short|long` env (UITestSeed.swift + ChatStore.debugSeedTranscript) bypasses the network (sets `connection.phase=.connected` + `sessions.activeStoredId` + seeds transcript via the real transcriptGeneration path) so ChatView renders short/long transcripts with NO gateway. Drive via: `SIMCTL_CHILD_HERMES_UITEST_SEED=long xcrun simctl launch`; DebugBridge port 9999 (boot token at <data>/tmp/gstack-ios-qa.token → /auth/rotate {new_token} → /session/acquire → /swipe {from_x,from_y,to_x,to_y} works via setContentOffset, /tap {x,y} works on SwiftUI buttons too [hamburger@~32,80 opened drawer; composer tap raised keyboard]); screenshot via simctl io; locate elements by PIL (bridge /elements can't see SwiftUI a11y ids). Pill is centered (HStack Spacer both sides) at x=196.

**SCROLL RESIDUAL — DEFERRED to bundle with offline-cache P3 wiring (user, 2026-06-08, accepting build 14 "not perfect, pass for now"):** build 14 STILL has intermittent open-scroll: sometimes opens at oldest, sometimes mid-conversation; manual scroll-to-bottom hits a WHITE VOID until you nudge up and it snaps to latest; not smooth. ROOT INSIGHT: the build-14 sim verification used a SYNCHRONOUS seed (debugSeedTranscript sets messages instantly) which works perfectly — but the REAL app seeds ASYNC from the network, so messages arrive AFTER the ScrollView has laid out at top, and the late transcriptGeneration scroll races variable-height rows still measuring. That async race is the residual bug; my synchronous harness never exercised it (verification gap, acknowledged to user). **UPDATE (2026-06-08): CACHE DOES NOT FIX SCROLL — PROVEN.** Extended the DEBUG harness (UITestSeed modes: long/short=sync, asynclong=~1ms deferred [cache-like], netlong=~250ms deferred [network-like]). RESULT: a deferred seed opens at the TOP at BOTH 250ms AND 1ms → it is a LAYOUT RACE, not latency: handleSeedScroll → scrollTo(edge:.bottom) fires synchronously on the transcriptGeneration onChange BEFORE the LazyVStack lays out the seeded rows, so it scrolls to the not-yet-expanded content's edge (≈top). A SYNCHRONOUS seed works only because `.initialOffset(.bottom)` resolves at first layout; the cache's seed is an async actor hop → same TOP-open bug. Cache P3 STILL shipped (db2c788af) for instant-paint value, but is NOT a scroll fix. THE REAL FIX (deterministic, no timer): drive the open-scroll off a CONTENT-SIZE-SETTLED signal (scroll to bottom when onScrollGeometryChange reports contentSize jumped after the seed lays out), not the layout-racing transcriptGeneration onChange. Deterministic repro now exists (netlong reproduces, asynclong = the cache case). **FIXED (SCROLL P0 r3, commit 55462f0c2, in build 15):** on a fresh open (.landOnNewest) ARM `pendingLandOnNewest`; BottomEdgeScroll re-pins to the bottom EDGE on every content-size change (seeded rows laying out) via ScrollPosition.scrollTo(edge:.bottom), fired POST-layout by onScrollGeometryChange → lands on newest after rows settle (no-op when content fits → short stays top). Pin released on move-away-after-landing (offset-driven `setAtBottom`, gated by `hasLandedOpen`) OR user .interacting → scrolled-up reader never yanked. VERIFIED deterministically in sim (screenshots): netlong/asynclong/sync-long all → #40; short → top; scroll-up → pill → tap → #40 + pill hides. UX1PolishTests 16/16. (ProtocolParity part-order test = pre-existing 120ms-sleep timing FLAKE — passes in isolation, fails under heavy parallel-build load; harden later.)

**BUILD 15 (2026-06-08, archiving):** the max-parallel drop. Commits: db2c788af cache P3 + harness, d15469f79 view wave (L12 a11y Dynamic Type/VoiceOver + craft + ABH-85 swipe, scroll-fenced), 04b73e31f desktop foreign-frame adoption, 55462f0c2 SCROLL P0 r3, 01fb0e270 build-15 bump. DEBUG harness HERMES_UITEST_SEED now has long/short/asynclong/netlong modes. Linear board reconciled. PENDING after build 15: desktop rebuild (user); L10 notifications/widgets device verify; offline P4 (backfill UX/eviction/offline-state); ABH-83 inline approvals; harden the flaky 120ms ProtocolParity test; the 5 Linear ambiguities (ABH-69 QuickCapture decision + ABH-70/76/77/78 device-verify).

**BUILD 16 (2026-06-08, archiving):** Build-15 QA (user) found: open-on-newest STILL broken ("linked to the load SEQUENCE — tap session → display"); pill works ✓; drawer swipe broken; instant-load fine. OPEN-ON-NEWEST: forensics (wf wegusr6q1) root-caused = open() sets activeStoredId=NEW then chat.reset() bumps transcriptGeneration with an EMPTY transcript, which handleSeedScroll classified (consumed the .landOnNewest, set lastSeededSessionID while empty → real seeds demoted to .keepPinned; empty reads as at-bottom → hasLandedOpen latched → real content growth disarmed the pin). FIX r4 (commits 822fbd967 + a269668de): (1) handleSeedScroll guards `messages.isEmpty` (skip empty frame); (2) removed the offset-driven disarm + hasLandedOpen — pin released ONLY by user drag (.interacting); (3) BottomEdgeScroll re-pins on content OR container size change. Net = open glued to newest through all settle (double-seed, variable-height rows, drawer animation), correct BY CONSTRUCTION. ⚠️ COULD NOT sim-reproduce the original even with a faithful drawer-tap repro (UITestSeed modes: switchlong, drawerswitch — both land #40 pre+post fix); the real trigger needs device conditions (cache→network double-seed w/ post-land height change, real markdown/image content). So r4 is a by-construction fix, NOT sim-confirmed → device QA is the confirmation. ACCEPTABLE (not 100%), PARKED ✅ — user (2026-06-08): build 16 scroll is "acceptable but not working 100% as expected"; choosing to MOVE PAST it to complete the whole product first, then fine-tune non-breaking polish later. So open-on-newest is PARKED, not fully closed — revisit in a later polish pass. The by-construction r4 fix got it to acceptable on real-device. LESSON: when a race is sim-unreproducible, fix BY CONSTRUCTION (re-pin every settle frame, release only on user gesture). SWIPE: reverted (user chose Option 2) — removed the view-wave .swipeActions (they never worked: card-drag latches any horizontal pan while drawer open; RootView dragGesture open-branch has no start-zone gate), kept long-press context menu. To make swipe work later = Option 1 (gate card-drag to card-originating drags). INFINITE SCROLL: removed "Load more" button → invisible auto-load probe, `.id` keyed on loadedCount so onAppear re-fires per page (continuous). Device QA for continuous-load. CACHE SCOPING DECISION (user, for P4): composite (serverId, profileId) scope key = option-1 mechanics; PROFILE switch (same server) = option 1 (isolated, kept, instant-paint); SERVER switch = option 3 (clear+repopulate); architected so dropping the clear-on-server-switch later = full option 1, no migration. Confirmed: offline-send queue EXISTS (QueueStore, persisted, drains on reconnect/turn/manual); offline-state ribbon EXISTS (ConnectionStatusBanner) [corrected my earlier wrong claims]; transcript retention 365d, eviction NOT wired (P4) = effectively kept; attachment cache policy = cache-on-access (downloaded/opened persist, else fetch-on-view) for P4.

**MODEL-PICKER REWORK (forensics wf wyb1img33, design locked 2026-06-08):** BUG confirmed — composer pill AND Settings both open the SAME ModelPickerView → both POST /api/model/set scope=main = GLOBAL profile-default write (gateway: "applies to NEW sessions only"); tapping the pill mid-chat does NOT hot-swap the running session. DESIGN: (1) Composer pill = SESSION hot-swap via WS `config.set` key="model" value="<model> --provider <p>" with session_id (or slash.exec /model, no --global) → agent.switch_model in place + session.info; NO gateway change. Native iOS-26 `Menu` popover (replace the full-screen sheet at ComposerView.swift ~176-191; anchor on modelChip), list from WS `model.options`(session_id). (2) Settings = GLOBAL default (keep /api/model/set scope=main) + global thinking/fast via WS config.set (no session_id; no REST setter exists). (3) THINKING/REASONING (effort Minimal→Max) + FAST toggles on BOTH surfaces, mirroring desktop ModelEditSubmenu. Capability flags {fast,reasoning} already reach iOS (ModelCapability); CURRENT state (reasoning_effort, fast=service_tier=="priority") is NOT decoded on iOS — must add reading it from session.info (server.py:2262-2265; desktop mirrors to $currentReasoningEffort/$currentFastMode). DECISION (user): session thinking/fast = PURE SESSION-ONLY → needs a SMALL gateway change (config.set reasoning [server.py ~6112] + fast [~5969] currently run _write_config_key UNCONDITIONALLY; gate it to skip the global write when session_id is present). Files: ComposerView, ModelPickerView, SettingsView, HermesGatewayClient (WS request already proven), RestClient+Control, ConnectionStore/SyncEngine (session.info state), tui_gateway/server.py (the small reasoning/fast seam). Linear: ABH-84 (exists, scope-expanded).

**GATEWAY ADDITIVE-MINIMIZATION (forensics wf wyb1img33):** ~2,065 modified-upstream-file lines can shrink to ~30-45 irreducible seam lines; ~1,360 lines move to NEW add-on files (hermes_cli/mobile_api.py + device_api.py routers via include_router [pattern already accepted: web_server.py _mount_plugin_api_routes + explicit include_router]; server.py push/broadcast/_git_branch_fast → new tui_gateway modules). IRREDUCIBLE upstream seams (~30-45 lines): _EMIT_SUBSCRIBERS hook in _emit, env-gated broadcast hook in write_json, session.delete evict extraction, ws.py owner-write F3 fix + session.delete auto-evict (land as standalone upstream PRs). DECISION (user): DEFER the refactor to PR-series time; keep the plan. (The 4 already-additive new modules push_notify/device_tokens/mobile_pair/audit_log stay as-is.)

**INFINITE SCROLL — still broken (user, build 16):** auto-load doesn't fire at bottom; manual load brings only a few. REAL REQUIREMENT: prefetch as the user NEARS the bottom (~1-2 screens early, not AT the bottom) loading a SUBSTANTIAL batch (≥30 sessions). Build-16 fix (invisible tail probe + small pageSize) insufficient. FIX (after P4, shares SessionStore): scroll-geometry near-bottom trigger (onScrollGeometryChange distance<~1.5 screens → loadMore) + bump the load batch to ≥30. Files: DrawerView + SessionStore.pageSize/loadMore.

**=== ABH-88 DE-PATCH PHASE 1 STARTED — CONTRACT WRITTEN (2026-06-10) ===**
Consolidation roadmap locked (user "go as per your recommendation"): **de-patch → upstream sync → topology B → polish batch → launch prep**. Phase-1 recon DONE + **CONTRACT-DEPATCH.md committed at repo root** (`abb6f8372`): inventory refresh = ~1,820 modified stock production lines (~900 PLUGIN / ~870 SEAM / ~50 DROP) + ~1,252 test lines (full per-hunk detail in ABH-88 Linear thread). KEYSTONE FINDINGS: (1) stock plugin system is the mount — `api` routers auto-mount `/api/plugins/<name>/` (dashboard_auth is itself a plugin!), facade has register_cli_command (kills main.py seam), register_dashboard_auth_provider (device-token auth as provider), pre_approval_request/post_approval_response + on_session_* hooks (pushes+audit); plugin API IDENTICAL base↔upstream/main = rebase-safe design target. (2) Upstream (549 commits ahead, base=3c231eb39 v0.16.0) still has NO multi-client broadcast (per-session transport routing only) → our fan-out = seam S1, flagship PR. (3) Upstream FIXED the model env-leak via `session["model_override"]` (≈ABH-145 free) → our fix DROPs at rebase; their pattern = template for reasoning/fast seam S4. CONTRACT: plugin pkg `plugins/hermes-mobile/` (api.py routers, push_engine, device_tokens, audit_log, mobile_pair, broadcast.py, gitbranch); seams S1-S10 each = upstream-PR candidate; iOS RestClient migrates to plugin paths behind capability probe; topology B = embedded desktop gateway loads same plugin (parity free; bind/pair UX later); waves W1(scaffold+move)→W2(seam-minimize conversions)→W3(iOS paths)→W4(verify pristine+seam ledger)→Phase-2 rebase. ABH-88 In Progress/High. NEXT SESSION: execute W1 per contract (fleet + adversarial gate, validate on test gateway 9124).


**SEARCH OVERHAUL SHIPPED + LIVE (2026-06-11, build 33 VALID, commit 38899c28b):** the last big public-beta item. SERVER (live on :9119 via 2nd careful redeploy after the interrupt fix): GET /api/sessions/search gains scope=all|messages|code → existing search_messages role_filter (messages→[user,assistant] prose, code→[tool], all→none); narrowed scope drops the session-id pass. Verified live: q=docker all=20, messages=6, code=20. CLIENT (build 33): RestClient.searchSessions(scope:) (default all, degrades on stock server); SessionStore.SearchScope + searchScope, re-runs on change; drawer segmented All/Messages/Code picker (appears while searching); jump-to-match = SessionStore.pendingSearchScroll set on result tap → ChatView scrolls to first message whose prose contains the query (falls through to land-on-newest for tool-only matches). iOS 657 + 238 web_server tests green. **PUBLIC-BETA STATUS: all QA items addressed.** Builds 30-33 all VALID: 30 (P0 freeze + photo picker), 31 (LA timer + archived dismiss + @-mention), 32 (device detail sheet + working-dir grey-out + automation drawer tab), 33 (search overhaul). Live :9119 carries the de-patch + interrupt-persistence fix + search scope. REMAINING (deferred, minor/optional): red trash icon (device-iterate cosmetic), sent-image thumbnail (feature/local-cache), partial-assistant-prose capture on interrupt (agent-core). Search jump-to-match only matches PROSE (tool-only hits fall through) — fine for v1. Punch list: apps/ios/QA-BUILD29-PUNCHLIST.md.

**INTERRUPT-PERSISTENCE FIX LIVE (2026-06-11, commit c82aaae78, redeployed to :9119):** data-integrity bug — interrupted/edited turns lost the USER prompt. ROOT CAUSE (Opus forensics): gateway reuses ONE AIAgent per session; its DB flush cursor _last_flushed_db_idx survives turns. The truncate-before-user-ordinal path (stop-and-resend / edit-retry) rewrote the DB via replace_messages() but never resynced the cursor (the /undo handler does). Next turn's _flush_messages_to_session_db computed flush_from from the stale cursor → skipped the user row (tool/assistant rows persist incrementally so they survived). FIX (gateway-only, no schema change): (1) resync agent._last_flushed_db_idx=len(truncated) after truncate replace_messages; (2) companion belt — after every terminal turn, idempotent replace_messages(full result["messages"]) + cursor resync so state.db == final messages on complete OR interrupted. 215 gateway tests pass; redeployed via launchctl kickstart after throwaway pre-flight; live verified. NOTE: partial assistant PROSE on mid-stream interrupt still not captured (never appended to messages before InterruptedError) — would need an agent-core change; lower priority. **Builds 30/31/32 all VALID on TestFlight** (P0 freeze, photo picker, LA timer, archived dismiss, @-mention, device detail sheet, working-dir grey-out, automation drawer tab). REMAINING for public beta: SEARCH OVERHAUL (3-way scope All/Messages/Code + jump-to-match + ranking — needs server FTS scope filter in hermes_state.search_messages + matched-message position in the endpoint, THEN client scope toggle + scroll-to-message + build 33 + another redeploy). Deferred minor: red trash icon (device-iterate), sent-image thumbnail (feature/local-cache). Punch list: apps/ios/QA-BUILD29-PUNCHLIST.md.

**BUILD 31 SHIPPED (2026-06-11, commit 802587f15):** three more pre-existing QA bugs fixed (none migration regressions). (1) LA timer [#8]: widget now renders Text(timerInterval:) counting locally from a startedAt: Date threaded through ContentState — advances even backgrounded/locked, no server push needed (was a static server-pushed Int frozen at 0). (2) Archived drawer-dismiss [#15]: onNavigate threaded into ArchivedSessionsView so tapping an archived chat closes the drawer too. (3) File-viewer @-mention [#4]: composer never wired onMentionFile → new MentionCompletion.appendMention (pure +4 unit tests) inserts @file:<path> + closes browser + refocuses. 657 iOS tests green; 31/31/31 gate; uploaded. RECLASSIFIED (not quick fixes): sent-image thumbnail = feature (server stores "Attached Image" text — needs image fetch or local cache); working-dir picker file-open = WAD (it's a directory picker). BUILD-32 CANDIDATES (need user steer / server / design): search overhaul (ranking + jump-to-match + verbose-scope toggle — confirm UX), interrupt persistence (gateway drops user msg+partial on interrupt — SERVER fix + careful redeploy), automation as drawer tab, device-row tap behavior, red trash icon (device iteration), sent-image thumbnail. Punch list: apps/ios/QA-BUILD29-PUNCHLIST.md.

**BUILD 30 SHIPPED + VALID (2026-06-11) — fixes the P0:** after the de-patch went live, full-app QA found 19 items (all pre-existing or QA-mode, none migration regressions EXCEPT the freeze). P0 = reconnect freeze + empty-transcript: Opus forensics traced it to MY W3 change — ConnectionStore.recoverActiveSession awaited capabilities.probe() INLINE, gating hydration behind ~8 @Observable mutations on resolvedPathStyle (which connection.rest reads), saturating the main actor while the always-mounted drawer was interactive. FIX (commit ca469c625): reconnect probe now fire-and-forget (matches proven initial-connect path) so hydration runs immediately; +nonisolated on the 5 ServerCapabilities static probe helpers (were inheriting @MainActor, serializing stage-2). Empty transcript shared the root, fixed by same change. No W3-gating/legacy-fallback regression. Also fixed photo-library picker (#1): PhotosPicker-in-Menu never presents on device → state-driven .photosPicker(isPresented:). 653 iOS tests green; build 30 archived+uploaded+VALID. DEFERRED to BUILD 31 (build-31 batch tasks tracked): #17 LA timer (widget renders static server-pushed elapsedSeconds → use Text(timerInterval:) local count + fix LA-token registration), #18 interrupt persistence (gateway drops user msg+partial on interrupt — SERVER fix, touches live; + stale composer label), #19 search overhaul (ranking + jump-to-match + scope toggle), #20 UX polish (red trash icon, archived-tap drawer-dismiss, device-row dead tap, sent-image thumbnail, 3-dot working-dir file open, @-in-viewer feedback, Automation as drawer tab). Full punch list: apps/ios/QA-BUILD29-PUNCHLIST.md. NEXT: user installs build 30, verifies freeze GONE + photo picker works; then build-31 batch.

**LIVE REDEPLOY DONE — DE-PATCH IS IN PRODUCTION (2026-06-11):** trunk feat/group-collapse-pin fast-forwarded to 61afe5ba8 (W1-W4 + ship); the live worktree at ~/.hermes/hermes-agent pulled it; ~/.hermes/config.yaml gained plugins.enabled: [hermes-mobile] (appended to existing anima-becoming; approvals/smart preserved; backup /tmp/config.yaml.predeploy-backup); pre-flight booted the merged code on the LIVE venv at :9126 (plugin mounted, routes 200, zero errors) BEFORE restarting; launchctl kickstart -k gui/$(id -u)/ai.hermes.dashboard. POST-RESTART VERIFIED on :9119: plugin route /api/plugins/hermes-mobile/devices 404→200, legacy /api/devices 200→404 (moved), sessions/status 200; agent-side wiring all live (hermes-mobile enabled, token-auth=1, resolve-observers=1, S1 fanout=1, S2 emit=1, ws observers=1, mobile-pair CLI registered); HERMES_GATEWAY_BROADCAST=1 + PUSH_ENABLED=1 intact. App (build 29) auto-flips to plugin paths on next reconnect (W3 force-reprobe on socket drop). **Phase 1 de-patch is fully shipped to production.** QA punch-list (build 29, all PRE-EXISTING not W3 regressions): photo-library picker won't open (PhotosPicker-in-Menu), sent-image shows "Attached Image" not thumbnail, 3-dot working-dir file won't open, @-in-viewer no effect, device-row dead tap, Mac→phone live mirror gap (foreign-frame), turn-complete push needs >30s turn (by design), LIVE ACTIVITY TIMER STUCK AT 0 (real bug — LA token registry empty, push token never registered/cleaned), APPROVAL CARD NEVER SEEN (user emphatic — investigate; smart mode auto-decides by design but verify manual-mode card renders on mobile end-to-end). NEXT: full guided step-by-step app QA → fix all bugs in one batch (build 30). Phase 2 upstream rebase after.

**TESTFLIGHT BUILD 29 SHIPPED + VALID (2026-06-11):** the Phase-1 de-patch app (plugin paths behind capability probe + legacy fallback) is on TestFlight — archive via ios-build.sh w/ ASC auth flags, bundle-version gate 29/29/29 across app+widgets+share, exportArchive destination=upload ("Uploaded HermesMobile"), ASC poll → processingState VALID (app id 6777140135). Exact procedure now pinned in apps/ios/SHIP-TESTFLIGHT.md (battle-tested commands incl. both key-dir variants). WHERE THINGS STAND: build 29 VALID; trunk merge of abh-88-depatch-w1 pending; live :9119 redeploy pending (do AFTER user installs build 29; needs plugins.enabled: [hermes-mobile] in config.yaml); then Phase 2 upstream rebase.

**ABH-88 W4 DONE (2026-06-11):** pristine-diff gate PASSED — 10-agent workflow (Sonnet classifiers per stock file + Opus completeness gate) classified EVERY residual hunk vs merge-base 3c231eb39 into the seam catalogue; zero plugin-internal leaks in stock code (sweep of 8 tokens across hermes_cli/tui_gateway/tools/gateway/agent); main.py = 0 diff. Deliverable: **SEAM-LEDGER.md at repo root** (fork-local) — per-seam tables, consumers, the S5 revocation TOCTOU known-limitation, drops, and the suggested upstream PR order (S3→S8→S6+S10→S7→S2→S1 flagship→S4→S5). Phase 1 of the de-patch is COMPLETE on `abh-88-depatch-w1`. ALSO: TestFlight ship credentials mapped and documented in **apps/ios/SHIP-TESTFLIGHT.md** (ASC key 3DHXXG4GHQ at ~/.appstoreconnect/private_keys/ = altool default path, machine-global; issuer d7deff8e-5489-4d18-995d-c8a10f854118; ExportOptions-TestFlight.plist in-repo; .p8 NEVER committed per [[api-key-scoping-discipline]]). REMAINING: build-29 archive+upload (needs user GO — release action) + device QA → trunk merge → coordinated rollout → Phase 2 rebase.

**ABH-88 W3 DONE (2026-06-11, commit `b77ba0764`, branch pushed to origin):** iOS client migrated to the plugin paths behind a capability probe. `APIPathStyle` (legacy|plugin) on RestClient — single `mobileAPIPrefix` swap covers all 11 moved call sites; default `.legacy`. `ServerCapabilities.pluginMount` probes FIRST at connect (absolute GET /api/plugins/hermes-mobile/devices), then the upload/fs/devices/profiles probes target the RESOLVED family (critical: otherwise a de-patched server 404s the legacy probes and hides those affordances). `cachedPathStyle(serverURL:)` (nonisolated static) serves background flows; self-healing alternate-family 404 retry on push register, LA register/unregister, and notification-action respond (404 ambiguous there). WS untouched. GATE: unit suite 653/0 via ios-build.sh (incl. 10 new PathStyleTests); live REST suite green vs BOTH a de-patched :9124 (plugin family, full device round-trip) AND a pre-de-patch trunk worktree :9125 (legacy family — fallback proven; lone red was the throwaway env missing python-multipart, fixed); UI suite env-gated tests skip clean. Build bumped to 29 (1.0.1). REMAINING PHASE 1: device QA + TestFlight archive/upload of build 29 (USER: signing/ASC; app is safe to ship BEFORE the gateway redeploy — fallback works both ways), then W4 (pristine-diff verify + seam ledger doc), then trunk merge + coordinated rollout (app first → live redeploy with plugins.enabled: [hermes-mobile] + HERMES_GATEWAY_BROADCAST env as today).

**ABH-88 W2 DONE (2026-06-10, same branch):** seam-minimization conversions complete. (a) W2a: `hermes mobile-pair` now plugin-registered via stock `register_cli_command`; module moved to plugin; **hermes_cli/main.py diff vs merge-base = ZERO**. (b) W2b: NEW additive `hermes_cli/dashboard_auth/token_auth.py` (TOKEN_AUTHENTICATORS/IDENTITY_VALIDATORS/SOCKET_OBSERVERS — the "pluggable dashboard token auth" upstream-PR shape); web_server + middleware device branches now generic registry loops; `device_tokens.py` moved to plugin (wired in `_wire_token_auth`); renames: `_ws_device_token_match`→`_ws_token_identity`, `_ws_active_device_id`→`_ws_active_identity`. (c) W2c: `audit_log.py` moved to plugin; tools/approval.py audit block → 10-line `_RESOLVE_OBSERVERS` seam (stock post_approval_response hook verified INSUFFICIENT — kwargs lack resolver identity); plugin `_wire_approval_audit` writes identical records. (d) W2d/S4: session fast/reasoning no longer force-build the lazy agent — overrides park on `session["fast_override"]/["reasoning_override"]` and apply in `_apply_session_agent_overrides` at build; pre-build status/toggle/config.get consult the parked override (ADVERSARIAL GATE caught the stuck-toggle/lying-status regression, reproduced + fixed + regression-tested); pre-build fast validation binds profile_home. GATE: trunk-parity on full suite (every failing file fails identically on clean trunk — incl. a PRE-EXISTING test_tui_gateway_server→test_approval timeout-test pollution under same-process runs); 9-agent adversarial workflow: 3 confirmed findings all fixed (S4 read-path ×2 dims, register(ctx) zero-coverage → new test_plugin_register.py with idempotency + degrade-to-stock + argparse round-trip), 2 refuted (ledgered: WS approval.respond device block ≈70 lines stays S5-coop until W4 ledger; TOCTOU note: revocation completing between _close_if_ws_device_revoked and notify_socket("register") leaves socket uncut until disconnect — pre-existing shape). Live smoke 9124: device-token issue→REST 200→revoke→401 matrix, audit read, mobile-pair QR v2 end-to-end via plugin CLI, W1 broadcast/LA smokes re-passed. Stock-file residual vs merge-base now: server.py S1-S4+S6 seams, ws.py S1+S7, web_server S5+S8+S10, approval.py ~12-line observer seam, hermes_state S8, main.py ZERO. NEXT: W3 (iOS RestClient → plugin paths behind capability probe + TestFlight build) then W4 (pristine-diff verify + seam ledger doc).

**ABH-88 W1 DONE (2026-06-10, branch `abh-88-depatch-w1` off trunk):** plugin package `plugins/hermes-mobile/` is live — `dashboard/api.py` (all mobile REST: upload, approvals respond/audit, devices CRUD, fs list/read, push register/LA — auto-mounted at `/api/plugins/hermes-mobile/`), `push_engine.py` (= old hermes_cli/push_notify.py + the server.py push/LA intake block; hermes_cli/push_notify.py DELETED), `broadcast.py` (fan-out engine: registry + per-transport bounded queue/drain + stored_session_id enrichment), `gitbranch.py`. Stock seams added: S1 = `server._EVENT_FANOUT_SUBSCRIBERS` (write_json post-owner-write) + `ws.TRANSPORT_OBSERVERS` (connect/disconnect); S2 = `server._EMIT_OBSERVERS` (_emit + synthetic session.finalize/session.deleted/session.interrupt); S3 unchanged (+1 line: `_runtime_sid` now ALSO set on the session.create row — fixed a latent trunk gap where LA tokens of created-not-resumed sessions never cleaned at finalize). `_git_branch_fast` is a 7-line shim → plugin w/ subprocess fallback. Stock-file diff vs merge-base: ~2,041 → 825 lines (residual = S4-S10 seams + W2 conversion targets: device_tokens/audit_log/mobile_pair stayed in hermes_cli until W2's provider/hook/CLI conversions). Tests moved to tests/plugins/hermes_mobile/ (conftest `load_plugin_module` + `wired_gateway`); also fixed 5 PRE-EXISTING trunk test failures (config.set tests still asserting pre-env-leak-fix behavior) + mobile_pair now tries plugin path then legacy. GATE PASSED: 774 tests green across all touched areas; full parallel suite = zero NEW failures (12 failing files + 9 collection errors reproduce identically on clean trunk — acp/anthropic_adapter/service-manager etc, environmental); live smoke on isolated :9124 (HERMES_HOME=/tmp/hermes-w1-gate): plugin loads, 10 routes mounted, auth-gated (401 w/o token), device issue + push/LA registry writes, S1 broadcast mirror w/ enrichment verified across 2 WS clients, S2 LA cleanup verified on session.close AND session.delete. DEPLOY NOTE: the live gateway needs `plugins.enabled: [hermes-mobile]` in config.yaml when this lands (routes mount regardless, but S1/S2 wiring + hooks need the plugin enabled). iOS still on legacy paths until W3 (legacy top-level routes are GONE from the de-patched server — do NOT redeploy live until W3's TestFlight build is out, or phone REST breaks; WS protocol unchanged). NEXT: W2 (CLI registration via register_cli_command, device-token auth as dashboard-auth provider, audit via post_approval_response hook, S4 rewrite to override-dict pattern).
**=== MODEL-PICKER ROOT CAUSE: STOCK GATEWAY ENV LEAK — FOUND/FIXED/DEPLOYED (2026-06-10) ===**
User's desktop repro (new session → pick GPT → /status shows "Sonnet 4.6 (openai-codex)") forensically root-caused on isolated gateway :9124 (WS probe /tmp/forensic-modelswap.py): **STOCK BUG** — `_apply_model_switch` unconditionally writes HERMES_MODEL/HERMES_INFERENCE_MODEL/HERMES_INFERENCE_PROVIDER/HERMES_TUI_PROVIDER into PROCESS env (TUI single-session semantics, #16857) → in the multi-session gateway every SESSION-scoped pick leaked into new sessions' defaults + ambient re-resolution of other sessions; model/provider could mix from two different swaps (the incoherent /status). PROVEN: swap A→gpt-5.5, new B started on gpt-5.5 while disk=gpt-5.4. ALSO proven the pre-first-turn swap itself WORKS at gateway level (turn usage shows picked model) — gateway core fine. FIX (`b9d6c5347` valencia / cherry-picked trunk): env writes gated to `persist_global or not sid`. VERIFIED post-fix (B starts on disk default; A's swap sticks incl. first turn). Live :9119 redeployed (also reset poisoned env). **Flagship upstream-PR candidate.** No iOS change needed; build 28 stays valid. Minor ledgered: session.status returns model/provider=None.
**ARCHITECTURE MANDATE (user, 2026-06-10):** iOS integration must work as a PLUGIN — no stock-file modifications; sustainable across upstream evolution; goal = public release + Nous adoption. = ABH-88 UN-DEFERRED as the next major workstream. **BOTH topologies must work natively:** (A) standalone gateway + desktop remote + iOS remote (current); (B) desktop EMBEDDED gateway (stock local mode — desktop spawns `python -m hermes_cli.main dashboard`, connection.json mode local; user's stock backup at connection.json.bak-local-mode) + iOS connecting to the desktop's embedded gateway (needs: reachable bind, port stability, pairing UX). De-patch target from prior forensics: ~2,065 modified stock lines → ~30-45 seam lines (each an upstreamable hook: _emit subscribers, write_json broadcast, include_router) + ~1,360 lines into plugin modules; 4 modules already additive (push_notify/device_tokens/mobile_pair/audit_log).

**=== BUILD 28 SHIPPED — PILL SESSION-TRUTH (2026-06-10) ===**
**Build 28 VALID** = ABH-84 build-27 QA fix: pill showed global default on fresh open + the PREVIOUS session's hot-swap after switching (picker was correct — it queries session-scoped model.options; the pill never got that truth). ROOT CAUSE: pill state written ONLY by session.info EVENTS; nothing wrote it on session SWITCH — open() didn't clear, and the session.resume echo's `info` (SessionRuntimeInfo already decodes model/provider/reasoningEffort/fast!) was discarded after confirmActiveProfile. FIX (zero new RPCs): open() now calls connection.clearSessionState() at switch; new `ConnectionStore.applyRuntimeInfo(SessionRuntimeInfo)` seeds pill state from the create/resume echo under openToken guards at all 3 activation sites (open resume ~1350, createDraftSession ~1423 before applyDraftSelection, branch-create ~1657). Tests 643/643 (+2: echo seeding incl. nil-no-clobber for lazy resumes; open-clears-prev-state). EXPECTATION: switching BACK to a swapped session shows its pick only while the gateway keeps that agent alive; a rebuilt agent legitimately reverts to global — pill now always agrees with the picker (that agreement WAS the bug).

**=== BUILDS 26+27 SHIPPED — RELEASE-HARDENING + BRANDING (2026-06-10) ===**
**Build 26 VALID** = release hardening: 9-surface parallel review (all-Sonnet finders, Opus-me verification — 5 reviewer claims REJECTED incl. the deliberate `Host: 127.0.0.1` push workaround + the loadMore shared-token design) → **17 verified fixes** (commit "release hardening"): URL scheme http/https-only; reconnect missing-config exit → .needsSetup+hasConnected=false (was stranding users); env-override #if DEBUG; double-respond guards (InboxItemRow/ClarifyBanner/CrossSessionBanner/SecurePromptView pre-await `sending`); MessageBubble positional segment ids (content-hash ids broke selection mid-stream); UsageSkillRow stable id; runInitialFill nil-total=keep-paging; chainTip immediate refresh; DrawerView first-load latch on visibleSessions; SharedInboxDrainer keeps queue on total failure; VoiceRecorder post-permission re-guard; photo-picker error surfaced; QueuedPromptRow commit-on-focus-loss; NotificationService flag-in-completion; SessionModelPopover apply-on-divergence guards (was firing spurious config.set on EVERY open since build 17) + real-family reinsert + refreshables; cron toolbar dup removed; Unarchive midground; NSLocalNetworkUsageDescription added. Full unit gate 641/641. Deferred+rejected items all in ABH-90 ledger comment.
**Build 27 VALID** = branding: **"Hermes Agent"** everywhere (user decision) — CFBundleDisplayName ×3 + all 6 usage strings; `hermes mobile-pair` stays lowercase (CLI literal).
**LIVE E2E (own gateway :9123): 4/7 passed — all 3 failures = test-gateway agent turns 401'd by the user's hermes-anthropic-proxy (:18802, rebuilt same day w/ nil-fingerprint-fail-closed).** App exonerated: connect/hydrate/session-create/drawer/settings/theme all machine-verified live. FLAGGED to user (their lane): fresh gateway instances can't auth through the rebuilt proxy. Test-gateway lesson in [[model-tiering-policy]]: isolated config home next time (model/set scope=main on a test instance writes the SHARED global config.yaml). iOS-26 audit: native shell (57 NavStack roots, 3 UIViewRepresentables), glass via ChromePill primitive + glassProminent/scrollEdgeEffect, 13 @available(iOS 26) gates w/ material fallbacks at iOS 17 floor.

**=== BUILD 25 SHIPPED (2026-06-10) ===**
TestFlight **build 25 VALID** (1.0.1(25)) — picker UI polish (user QA of 24): (1) **Edit Models → nav bar** top-leading "Edit" text button (standard iOS curation placement; Done keeps trailing); bottom list row removed. (2) **Thinking/Fast → per-model accordion**: pinned top sections removed; tapping a model selects it AND expands it in place (`expandedConfigRows` under the current family row, `.snappy(0.25)` withAnimation on all selection mutations incl. rollback + onChange) showing only that model's supported config (reasoning picker if caps.reasoning, fast toggle if caps.fast||family.fastId). Tests 15/15. Ship: valencia `eff80ddfd`, trunk bump `e478e9778`. QA train 23→24→25 all pending ONE device pass (provider-identity/Edit-Models/family-rows + draft-pick/stale-pill + nav-bar-Edit/accordion).

**=== BUILD 24 SHIPPED (2026-06-10) ===**
TestFlight **build 24 VALID** (1.0.1(24)) — ABH-84 follow-up: **model pick at ANY point, drafts included.** Draft (no session) pick PENDS as `DraftModelSelection` on ConnectionStore (model/provider + thinking/fast if touched; pill shows it via `draftModelShortName`); `SessionStore.createDraftSession` applies it to the new session_id right after session.create and BEFORE `ChatStore.send` submits (send fully awaits create — verified) → FIRST turn runs on the pick (config.set key=model builds the agent). Best-effort apply (never blocks/loses the message); pend cleared on apply/startDraft/open(existing)/teardown. BONUS root-cause fix: `startDraft` now calls `connection.clearSessionState()` — draft pill no longer shows the LAST chat's model (pre-existing stale-pill bug). Picker: `sessionId: String?` (nil=draft mode → selections pend, no RPC; REST-global options; caption "Applies when this chat starts"); ComposerView hint-sheet branch REMOVED (picker always opens). Tests 15/15. Ship commits: valencia `3d9b84f70`, canonical trunk bump on top of build-23 chain. NO gateway change in 24 (23's provider field already live). DEVICE QA (24): new chat → pick model → pill shows pick → send → first reply on picked model; abandoned-draft pick doesn't leak; build-23 items (dual-checkmark gone, Edit Models, family rows).

**=== BUILD 23 SHIPPED (2026-06-10, from Conductor workspace valencia) ===**
TestFlight **build 23 VALID** (1.0.1(23)) — ABH-84 device-QA fixes: (1) DUAL-CHECKMARK BUG — selection identity was bare model name; now the (provider, model-family) PAIR via `ModelVisibility.currentFamilyId` (desktop model-menu-panel parity); picker loads session-scoped WS `model.options` (session-true current; REST fallback); ConnectionStore gains `sessionModelRaw`+`sessionProvider`; gateway `_session_info` gains `"provider"` (1-line, REDEPLOYED + LIVE — dashboard restarted twice 2026-06-09/10 with user authorization: ABH-84 emit, then provider field; new PID verified HTTP 200 both times). (2) EDITABLE MODEL LIST — `ModelVisibility` (UserDefaults `provider::model` keys, nil=top-50/provider default, exact desktop model-visibility.ts port incl. case-insensitive `-fast` family collapse — STRICTLY BETTER than desktop, whose exact-case sibling lookup drops mixed-case fast variants; logged to ABH-90 ledger) + `ModelVisibilityEditorView` sheet (search/toggles/Reset) from the picker; current model always shown. Tests: ModelVisibilityTests 12/12 (incl. the exact dual-checkmark repro + session.info raw/provider decode). Ship: trunk `feat/group-collapse-pin` @ 09865c4f6 (bump) on canonical + mirror; valencia branch study-session-jsonl @ 409b6a718. **Build discipline now: iOS builds ONLY via `scripts/ios-build.sh`** (machine-global mutex, per-worktree DerivedData, SIGTERM-only) — used for build+tests+archive of 23, worked end-to-end. Memory now travels in-repo at `.agent-memory/` (+ CONDUCTOR-BOOTSTRAP.md); repo-key memory dir symlinked to home memory 2026-06-09. PENDING DEVICE QA (build 23): dual-checkmark gone; pill reflects hot-swap; Edit Models curation; family rows (no -fast duplicates).

**=== POST-COMPACTION CONTINUITY HANDOFF (2026-06-08, OVERNIGHT UPDATE) — READ THIS FIRST ===**
CURRENT SHIPPED: TestFlight **build 21 VALID** (1.0.1(21), bump commit e4a56bb09) — confirmed VALID, on user's phone for QA. HEAD = e4a56bb09; tree clean. FEATURE FREEZE declared; CONSOLIDATION/FINALIZE phase started (review/bug-hunt wave running, workflow ww7yc81w0). Build 21 = build 20 + the **drawer automation-bifurcation** feature (user-approved "build it"): Recents = human-chat-only (cron excluded), automation runs get their own feed, workspace groups collapsible/pinnable. **THIS IS THE LAST NEW FEATURE → next = consolidation/finalize phase.** Build 20 (prior VALID) = build 19 + the 4 HIGH-priority bug fixes: **ABH-78** banner contrast (background.contrastingForeground, 32582b9ab), **ABH-77** Usage empty state (was already present — verified), **ABH-76** cron error/prompt + copy contextMenu (32582b9ab), **ABH-86** drawer optimistic re-sort on send (ad824c419). Prior VALID: 17 (4-feature drop), 18 (+infinite-scroll review fixes), 19 (+cross-session banner). HEAD = b2c4c6956; working tree clean. Branch hermes-mobile, NEVER push. 613 unit tests green; offline UI suite green (7 live UI tests self-skip — need a gateway).
**DRAWER BIFURCATION — BUILT (build 21, 2026-06-08).** User approved "build it / move forward with your recommendation." Commits: `78c133b3c` (data layer: gateway `/api/sessions`+`/api/profiles/sessions` gain `source`/`exclude_source` params [list_sessions_rich already had exclude_sources; added it to session_count]; iOS RestClient `excludeSource`/`source`; SessionStore.recentsExcludeSources=["cron"] threaded through all fetch rails), `23f5ac51a` (UI: AutomationRunsView cron feed reachable from CronJobsView 'Recent runs' + opens via sessionStore.open+dismiss; workspace groups collapsible[DefaultsKeys.collapsedWorkspaces]+pinnable[pinnedWorkspaces]; hideCron toggle removed), `90d69e863` (human-only Recents BY CONSTRUCTION — visibleSessions always filters cron + AutomationRunsView client-filters cron, so correct EVEN without a gateway redeploy). 626 unit tests green. ⚠️ GATEWAY REDEPLOY (restart 9119) unlocks EFFICIENCY (server excludes cron → not fetched/cached) + server-filters the runs feed — but correctness does NOT depend on it (client filters guarantee it). Manual cross-cwd folders = PARKED (Wave-4; need server-side persistence). Original discussion below (now resolved):
**OPEN DESIGN DISCUSSION — DRAWER (user raised 2026-06-08, "discuss before doing"):** (1) BIFURCATE automations from the chat feed: cron sessions (source=="cron", = automation RUN history) are interleaved into Recents + filtered CLIENT-side (hideCron); gateway `/api/sessions` has NO source filter (only limit/offset/min_messages/archived/order) so we fetch cron-heavy windows + (confirmed) DO cache them (persistSessionListToCache saves full `sessions` incl cron) = waste. ("Scroll doesn't move" already fixed in build 18/ABH-89 ≥30-visible paging; the WASTE is the real issue.) RECOMMENDED: option A (add small additive `source`/`exclude=cron` param to /api/sessions + list_sessions_rich) + option C (Recents=human-only; automation runs live under the Automations panel/CronJobsView per-job history, own cache or fetch-on-view). (2) GROUP-BY-WORKSPACE today = AUTO by `cwd` (workspaceKey=trimmed cwd); user asks about MANUAL project folders (Claude Projects / Codex style) — RECOMMENDED: enhance cwd-as-project (rename/pin/collapse) first; manual cross-cwd folders need SERVER-SIDE persistence to sync desktop+mobile (Wave-4 sized), park until wanted. AWAITING USER on 4 Qs: drawer human-only?; OK to add gateway source param?; cwd-grouping enough vs manual folders?; cache automation runs or fetch-on-view? DO NOT build until user answers.

**HIGH-PRIORITY BUGS (build 20):** ABH-78/77/76 → DONE in Linear. ABH-86 → In Progress (symptom 2 re-sort FIXED + unit-proven via SessionStore.noteActivity optimistic lastActive bump [SessionSummary.lastActive let→var] + mergeSessionPage max-carry-forward + ConnectionStore.route hook on message.start/complete; symptom 1 already covered by scheduleSessionRefresh; symptom 3 already worked via TimelineView; residual = gateway-side real-time for EMPTY remote sessions, out of scope) — keep In Progress for device-QA "send → jumps to top" feel.
**DESKTOP LIVE-SYNC: DONE (2026-06-08).** User said "You rebuild" → I rebuilt apps/desktop (npm run build + `pack` = electron-builder --dir, notary creds unset so notarize cleanly skips, signed Apple Development Y8FTEKXXF6, codesign VALID), quit the running packaged app, backed up the old (`/tmp/Hermes.app.prebuild-bak`), installed the new build to `/Applications/Hermes.app` (v0.15.1, same version = code update; asar confirms the foreign-frame fix: stored_session_id usages 2 vs old 1), relaunched via `open` → RUNNING + reconnected to 9119 (ESTABLISHED 61088→9119). The 9119 dashboard SERVICE was untouched (only the client app restarted). Desktop foreign-frame adoption (04b73e31f) is now LIVE. No longer pending.
**ABH-84 GATEWAY SCOPING — RESOLVED + HARDENED (2026-06-08).** User noted desktop already does per-session model/reasoning/speed without touching global. INVESTIGATED + PROVED (ran the real gateway fns): `config.set reasoning/fast` → `_write_config_key` → `_save_cfg`, and `_save_cfg` ALWAYS writes the GLOBAL `_hermes_home/config.yaml` (server.py:1448-1451) — `_load_cfg` (1417) honors the per-session profile override but the WRITE does not (asymmetry). Proof: with a profile override active, `_write_config_key("agent.reasoning_effort","high")` wrote GLOBAL config.yaml→high, profile stayed medium. SO: model IS genuinely session-only on both clients (slash `/model` no --global; iOS `config.set key=model`→_apply_model_switch no persist). BUT reasoning/fast via config.set write the global default — so the DESKTOP today ALSO writes global for reasoning/fast (it just looks per-session: applies to the live agent immediately + doesn't retroactively change other open sessions; shows up as a new chat's default). The seam (skip _write_config_key when session_id present) is CORRECT + KEPT — it makes reasoning/fast genuinely session-only, and since desktop+iOS send identical config.set, the one seam fixes BOTH. EDGE HARDENED (commit 2d425bc3b): config.set reasoning/fast now BUILDS the session agent if absent (reuses _start_agent_build/_wait_agent like key=="model") so a toggle before the first turn applies instead of dropping. py_compile clean. **⚠️ REQUIRES GATEWAY REDEPLOY (restart 9119) to go live — NOT done (never restart 9119; user redeploys). iOS binary unchanged (build 19 already sends the right config.set) — no new TestFlight build needed.**
**BUILD-17 ADVERSARIAL REVIEW (workflow w30v4c4r0, 9 agents, 4 features × finder+verify):** 5 confirmed findings. FIXED in build 18 (commit 47d45a322): (P1) grouped-workspace layout had NO near-bottom prefetch (only tail sentinel) → grouped rows now prefetch; (P2) prefetch used a captured enumeration index vs live count (desync on re-sort/filter) → `maybePrefetchMore(rowId:)` now resolves the row's CURRENT index by stable id against live unpinnedSessions. DOCUMENTED, NOT fixed (your call, on ABH-84): (P2) `config.set reasoning/fast` with a session whose gateway agent isn't built yet (`agent is None`) silently drops the setting — a small regression from the seam, but ~unreachable from the iOS popover (gates on activeRuntimeId⇒post-first-turn⇒agent exists) + benign global-default fallback; fix = ~14 lines reusing the model-path's _start_agent_build, deferred per minimize-gateway. SKIPPED (already safe): 2 applySessionInfo nil/empty nits (every read is `?? ""`).

**LANDED THIS SESSION (committed on hermes-mobile, all in build 17):**
1. **Offline cache P4** (`11a416615`) — per-(server,profile) scope: migration v2 (append-only ALTER + legacy-sentinel backfill; fingerprint v1→v2 → nuke-rebuild fallback for un-ALTERable DBs); PROFILE switch KEEPS isolated rows (instant re-paint), SERVER switch CLEARS via the SOLE `clearSessionsForOtherServers` call (drop it later = full option-1, NO migration). Attachment cache-on-access (new `AttachmentBlobCache.swift` + `FileViewerView.loadImage` hook; opened/downloaded images persist on disk, else fetch-on-view). Cache-miss/no-cache path verified BYTE-IDENTICAL at every seam. 42 cache + 81 store tests green. Eviction (365d) DEFINED but NOT wired (kept).
2. **Drawer infinite scroll** (`8b24bba85`, Linear ABH-89) — `loadMore()` now pages until ≥30 NEW VISIBLE rows (`loadMorePageVisibleTarget`) OR server-exhausted (no-progress guard), routed through `resolvedInitialFillFetch` (same rail as the proven fill; previously used `sessionsWithTotal` even on the aggregate rail = latent bug, now consistent). Near-bottom PREFETCH in DrawerView fires `loadMore` when a row ≤`loadMorePrefetchDistance` (12) from the end appears; tail sentinel kept as backstop. +2 deterministic unit tests (cron-heavy ≥30, ungrowable-window exhaustion).
3. **Inline approvals** (`1e07f8c99`, ABH-83) — `ApprovalCard` injected at the transcript tail (ADDITIVE LazyVStack row, scroll-fence respected — verified only a comment references fence symbols); floating open-session banner removed. OPEN: ABH-83's "cross-session pending banner" for NON-open sessions — commented on the issue, awaiting user (is the Inbox enough?).
4. **Model-picker session hot-swap** (`36d125518`, ABH-84) — composer pill → compact native `.popover` (`SessionModelPopover`) hot-swaps model / Thinking-effort / Fast for the LIVE SESSION via WS `config.set`+`session_id`; Settings keeps the global-default picker + gains global Thinking/Fast. GATEWAY SEAM (local-only, additive): `config.set` fast(`~5969`)/reasoning(`~6108`) skip the global `_write_config_key` when `session_id` present + reasoning gained a `session.info` emit. INTEGRATION FIX (Opus gate): session model value = `"<model> --provider <slug>"` (the robust switch_model explicit-provider path, matching desktop) — builder's slash-joined `"provider/model"` only resolves on aggregators; reasoning "Off"→`"none"` on both surfaces.
+ flaky-test de-flake (`23cdb2f8d`): the two ProtocolParity parts-order tests now poll (`waitUntil` helper) instead of a fixed 120ms sleep that raced the debounce flush. 22/22.

**DEVICE-QA PENDING (build 17 — ping user):** (a) infinite scroll "auto-loads ≥30 as you NEAR the bottom" on a real >30-session account; (b) inline-approval round-trip (real tool call → card → approve/deny → ACK) — needs live gateway; (c) model pill hot-swaps the LIVE session (not the global default) + Thinking/Fast apply per-session + Settings still drives new chats; (d) confirm P4 didn't regress the parked open-on-newest scroll. Worktree builder branches `feat/inline-approvals`+`feat/model-picker` still exist (integrated; safe to prune). DESKTOP live-sync fix still needs USER rebuild.

**DECISIONS LOCKED:** session thinking/fast = pure-session (tiny gateway seam, DONE); gateway additive-min DEFERRED to PR-series time (ABH-88 holds plan); cache scope = composite (serverId,profileId), profile-keep/server-clear, upgradeable to full-option-1 by dropping the server-clear.
**LINEAR:** ABH-89 In Progress (infinite scroll, commented w/ commit `8b24bba85`), ABH-84 In Progress (model picker, commented), ABH-83 In Progress (inline cards done, cross-session banner OPEN Q commented), ABH-85 Backlog (swipe reverted), ABH-88 Backlog (gateway-min deferred), ABH-55 offline (P3+P4 DONE), ABH-72 Done (a11y).
**TOOLING:** DEBUG harness UITestSeed modes long/short/asynclong/netlong/switchlong/drawerswitch (HERMES_UITEST_SEED env) for sim scroll verify; /tmp/asc.mjs ASC poll helper (key 3DHXXG4GHQ, issuer d7deff8e-5489-4d18-995d-c8a10f854118, app 6777140135); TF pipeline = bump CURRENT_PROJECT_VERSION → xcodegen → archive (ASC auth flags) → PlistBuddy gate → exportArchive ExportOptions-TestFlight.plist → poll VALID. Sim BC5EB32A iPhone 17 Pro. Bridge port 9999 (auth/rotate→acquire→tap/swipe; programmatic swipe ≠ .interacting so can't test scroll-up-disarm or swipeActions-reveal — device-QA those).

**MAX-PARALLEL PUSH IN FLIGHT (2026-06-08, user chose "max parallel"):** 4 lanes, isolated to avoid build/commit collisions (build collision = two iOS workflows can't share one build tree). LANE STATUS: (1) DESKTOP live-sync foreign-frame adoption — DONE+COMMITTED `04b73e31f` (repair round ok:true: lock-lifecycle PASS + correctness/regression PASS + tsc clean; both sync halves now done — iOS in build 14, desktop committed; USER must rebuild desktop locally, source-only, no 9119 restart). (2) VIEW WAVE workflow (wdtu18syz, main tree) — L12 a11y (Dynamic Type + VoiceOver) + craft polish + ABH-85 drawer swipe across view files, builders file-partitioned, integrator builds+commits; HARD-FENCED off the stabilized scroll/keyboard/drawer machinery in ChatView/RootView. (3) CACHE P3 wiring — Opus agent in MANUAL worktree /tmp/hermes-cache-p3 (auto worktree-isolation failed "resolve base branch HEAD"; created via git worktree add --detach + copied untracked CONTRACT-OFFLINE-CACHE.md in); read-through/write-through into ChatStore/SessionStore/ConnectionStore, synchronous cache seed (also the deferred scroll fix's mechanism — human verifies scroll w/ async-harness after); leaves uncommitted in worktree for review. (4) LINEAR reconciliation agent — board ABH-59/60..72/73..87 vs build 14 shipped. INTEGRATION: I merge each lane sequentially as it returns (view wave commits to main itself; cache worktree diff I review+apply+build+scroll-verify; desktop done). Build-14 = HEAD base f098a58f8; desktop commit on top 04b73e31f.

PENDING (post-build-13): (a) DONE — desktop fix committed 04b73e31f (tell user: rebuild desktop); (b) HELD craft batches for build 14 — ChatView (CC-03 new-msg anim, VC-06 toast edge, VC-11/12, ESC-09) + RootView (ESC-01 corner-snap, ESC-02 phase transitions, DC-10) — these codes live in LINEAR not repo docs, query before briefing; (c) L12 a11y sweep (76 hardcoded fonts, contrast, labels); (d) L10 notifications/widgets device verify; (e) parked ABH-85 swipe actions + ABH-83 inline approvals; (f) offline P3+P4 (LAST before public release); (g) Linear ABH-73..87 reconciliation. Build 13 QA walk pinged to user (headline: does scroll FEEL natural; keyboard rises with transcript; keyboard dismisses on drawer-open).

OPEN USER DECISION: offered an auto-deny settings rule for mcp__computer-use__* (so stray calls never block when user is away) — AWAITING user yes/no.

STILL QUEUED (post-build-13): L12 a11y sweep (76 hardcoded font sizes, contrast, VoiceOver labels — touches many view files, run when chat files settle); L10 notifications/widgets (device-only verify session w/ user); parked features ABH-85 swipe actions + ABH-83 inline approvals; then offline P3+P4. Linear ABH-73..87 needs reconciliation. User-reported bugs not yet fixed are tracked in apps/ios/POLISH-NOTES-USER.md.

**Wave 3 (locked 2026-06-05): Hardening → Upstream.** 3a security: per-device tokens + revocation panel (replaces single shared token — server registry w/ device names), audit log of approvals per device, token rotation/re-pair flow. 3b upstream to NousResearch (user ACTIVELY wants this): license/CLA check, clean PR series (app / upload endpoint / broadcast / push infra separable), generalize setup (remove user-specific assumptions), apps/ios README + pairing docs, open a discussion with Nous BEFORE the big PR; embedded Tailscale (TailscaleKit) decision rides with it — v1 PR may require the Tailscale app. Wave 4 candidates: Mission Control iPad console (kanban/goals/unified inbox), ambient voice mode.

**Topology:** shared persistent dashboard `ai.hermes.dashboard` (launchd) on 127.0.0.1:9119, wrapper `~/.hermes/bin/hermes-dashboard-service` reads token from `~/.hermes/dashboard.token` (0600 — never in plist, per [[api-key-scoping-discipline]]). Tailscale Serve `:9443` → 9119 (tailnet-only) for iOS; desktop should use remote mode at `http://127.0.0.1:9119`. Clients MUST send `Host: 127.0.0.1` header (server Host-check rejects the ts.net hostname; verified) — note Mac-local DNS resolves ts.net names to public Funnel IPs (broken MagicDNS), but iPhone/iPad resolve correctly via Tailscale app.

**App:** apps/ios/HermesMobile (XcodeGen; regenerate with `xcodegen generate` after adding files). JSON-RPC WS client actor + @Observable stores + chat UI. Dev override: HERMES_URL/HERMES_TOKEN env (TEST_RUNNER_ prefix for xcodebuild tests, SIMCTL_CHILD_ for simctl launch). Live UI test does a real Opus turn against 9119.

**Backend patches on the branch:** `POST /api/upload` in hermes_cli/web_server.py (multipart → ~/.hermes/uploads, feeds path-based image.attach; HEIC rejected — iOS converts to JPEG); multi-client broadcast in tui_gateway (ws.py live-transport registry + server.write_json fan-out, opt-in via HERMES_GATEWAY_BROADCAST=1 set in the wrapper; frames enriched with stored_session_id).

**Status 2026-06-05 (wave 1 complete):** ~30 features shipped, 95 tests green. Paid team **6J4Y9NKRQ2**, bundle id **ai.hermes.app** (old ai.hermes.mobile was claimed by the free team — old app should be deleted from phone), 1-year signing. Three targets: app + HermesWidgets (status/usage widgets + HermesTurnLiveActivity) + HermesShare (share sheet → app-group inbox). Features: voice dictation (/api/audio/transcribe), TTS speak, edit/retry (truncate ordinals), queue/offline outbox, syntax-highlighted code + ANSI, session search/pin/rename/archive/export/cron-filter, 6 control panels, approval inbox (broadcast-fed, badge + iPad inspector), Siri App Intents, Face ID lock, VisionKit scanner, Tailscale hint, Spotlight, GBrain quick-capture, iPad 3-column, hermesapp:// deep links, app group group.ai.hermes.app. **Push LIVE (verified on device 2026-06-05)**: APNs key TQQF7DKKX8 at ~/.hermes/apns-key.p8 (0600), armed via env in the dashboard wrapper (HERMES_PUSH_ENABLED=1, team 6J4Y9NKRQ2, topic ai.hermes.app, SANDBOX gateway — dev-signed builds; flip HERMES_APNS_USE_SANDBOX=0 for TestFlight). Gateway hook: tui_gateway/server.py _emit→_push_hook pushes approval.request, clarify.request, and >30s message.complete. Device tokens: ~/.hermes/push_tokens.json via /api/push/register. App toggle: Settings → Notifications. Test send: `venv/bin/python -c "from hermes_cli.push_notify import notify; notify('test','t','b')"` with env set. Known flake: ChatFlowUITests intermittent under concurrent load (gateway session.create stall, not an app bug).

**Deployed 2026-06-05:** Desktop switched to remote mode (connection.json → http://127.0.0.1:9119, token encoding "plain"; backup at connection.json.bak-local-mode) — verified ESTABLISHED socket to :9119, no private backend spawned. App installed on iPhone Air via `devicectl device install` over network CoreDevice tunnel. Signing: the active personal team is **BQRGNM43SM** (NOT the old cert's Y8FTEKXXF6); building with `-destination 'platform=iOS,id=00008150-000911CA0240401C' -allowProvisioningUpdates` registers the device + mints the profile. Free-team profile expires every **7 days** → reinstall: `xcodebuild build` + `devicectl device install app --device 1D51F2FB-1DB2-52BF-818B-EFD2ACE7C0E7 .../Debug-iphoneos/HermesMobile.app`. User must trust the developer profile once (Settings → General → VPN & Device Management). iPad not paired.
