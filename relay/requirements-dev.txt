# Dev/venv note — hermes-relay
#
# Build all venvs on the external volume (/Volumes/MainData); the internal disk
# is tight. Python is Homebrew 3.13.
#
#   /opt/homebrew/bin/python3.13 -m venv /Volumes/MainData/Developer/hermes-tmp/venvs/relay
#   source /Volumes/MainData/Developer/hermes-tmp/venvs/relay/bin/activate
#   pip install -r requirements-dev.txt
#   # or: pip install -e '.[dev]'   (uses pyproject)
#
# Unit tests mock every external transport and never touch the network.
# End-to-end tests use a stock isolated Gateway on port 9123+ and a temporary
# HERMES_HOME. Production HRP/2 may use the real co-located loopback 9119
# Gateway, but automated tests and probes never do.
#
# Legacy v1 notifier/replay plumbing is imported from plugins/hermes-mobile via
# hermes_relay.plugin_bridge. HRP/2's trust boundary lives in hermes_relay.v2.

# runtime
websockets==15.0.1
httpx==0.28.1
pyyaml==6.0.3
pyhpke==0.6.5
cryptography==48.0.1

# dev/test
pytest==9.1.1
pytest-asyncio==1.4.0
