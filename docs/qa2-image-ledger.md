# QA-2 Image-Forensics Ledger (consolidated)

**Source evidence:** 48 screenshots, `~/Downloads/IMG_2529.PNG … IMG_2576.PNG`, captured 02:14–02:37 local on 2026-07-21 on iPhone Air (build 115).
**Coverage of this ledger:** IMG_2529–IMG_2549 (21 images, first two forensics batches). IMG_2550–IMG_2576 not yet forensics-processed — append batches here as they are read.
**Cross-reference:** bug ledger R1–R16 in `docs/DAILY-DRIVER-QA2-SPEC.md`. Relay log: `~/Library/Logs/Hermes/relay.log` (APNs attempts 02:27:46, 02:35:29–30, 02:36:52–53).

## NEW defects observed that are NOT in the R1–R16 ledger (track for triage)

- **N1 Nav-bar ghost-text bleed (pervasive, IMG_2531–2541+):** stale prior-turn content renders un-clipped underneath/behind the translucent "New chat" nav bar, overlapping the title and icon row. Worst at IMG_2536/2539 (three lines, overlapping the gear/pencil/overflow icons). Looks like the transcript scroll content is not clipped to the safe-area-inset nav bar region — a clipping/`.ignoresSafeArea`/z-order bug in the chat surface, independent of R5/R6.
- **N2 `tool.generating` / `review.summary` raw internal state names flash in UI (IMG_2538, 2540, 2545, 2546):** transient (≈1s) raw tool-state string shown with a green ✓ checkmark, resolving later to the friendly name (`clarify`). Fallback label before tool resolves; also a C1 (native polish) violation.
- **N3 Premature clarify-card dismissal (IMG_2535, 2540 — reproduced twice):** interactive Yes/No card vanishes and collapses to a settled `✓ clarify` row BEFORE any user answer exists in the transcript. Card-lifecycle ordering bug adjacent to R8 (collapse happening pre-answer).
- **N4 Live-turn duplicate thinking print (IMG_2533, 2536, 2538, 2541):** the same thinking sentence renders twice — once as a truncated collapsed summary row (with per-item timer), once in full as un-collapsed italic body text directly beneath. Collapse contract violated for the live turn while the settled turn above it collapses correctly.
- **N5 Composer send-button icon swap while offline+text (IMG_2548):** with "Not connected" banner up and draft text "Hi" typed, the trailing composer action renders as a bullet-list (task-list) glyph instead of the send arrow — wrong icon-state mapping or z-order collision between send and task-list controls.
- **N6 Per-item timers on expanded thinking rows (IMG_2533 3s → 2535 18s; 2538 1s → 2540 17s):** confirms R5's "timer is per-TURN" note — these count a single sub-step, not the turn.

## Per-image findings (batch 1: IMG_2529–IMG_2540)

| Image | Time | Screen | Key findings | Bugs |
|---|---|---|---|---|
| IMG_2529 | 2:13 | Sidebar drawer (175 chats) | Drawer does NOT cover full width: opaque session list only left ~75% (0–690 of 921 displayed px); right ~25% strip shows a second dimmer layer bleeding through — duplicate hamburger circle, two skeleton-shimmer clusters, faint `Aheli · /Users…` path text, partial composer ('Message', blue + button, 'qwe'). Blue "New chat" pill straddles both layers. Compositing/coverage bug — not a clean full-cover slide-over; destination chat view (stuck on skeleton) visible before any dismiss gesture. (Battery red — device context, not a bug.) | R2 |
| IMG_2530 | 2:13 | Session load skeleton | Full-screen shimmer skeleton; no header title; footer breadcrumb `Aheli · /Users/abbhinnav/Documents/02-Businesses/Aheli` already resolved while transcript above is still skeleton; composer fully interactive. Chrome resolves before transcript → visibly half-loaded state. | R3 |
| IMG_2531 | 2:15 | Settled turn + focused composer | Nav-bar ghost text `Hey Abhi. What's up?` bleeds behind "New chat" title bar (N1). Settled `Worked >` single row is correct. Token footer `52568 tokens · ctx 52.5K`. No clarify card (R9 n/a). | NEW (N1) |
| IMG_2532 | 2:17 | Live turn, streamed reply | Ghost text again (N1). Live-turn stacking: `Worked >` + separate `⟳ Reading https://getdaytrends.com/singapore/ >` tool row + lone blue streaming-cursor block on its own line = three stacked rows where one collapsed "Working…" line is required (R5). ~70px gaps between user bubble→Worked and tool row→cursor (R6). RED STOP button present in composer (stop affordance present here, contra R4's sometimes-missing). | R5, R6, NEW |
| IMG_2533 | 2:19 | Clarify generating (thinking + Asking row) | **The 02:19 photo cited by spec for R6.** Ghost text (N1). Italic thinking row with per-item timer `3s ⌄` (expanded, not collapsed — R5 violation: per-tool timer, not per-turn), then same sentence repeated in full below (N4 double-print). Then ~320px dead band before `⟳ Asking Should I set up…monitor that only pi… >` + cursor. Clearest R6 evidence: reserved/oversized empty container between thinking block and tool row. Keyboard open, no card yet. | R5, R6, NEW |
| IMG_2534 | 2:19 | Clarify card rendered (Yes/No) | **R7:** flat dark-gray rounded rect, blue speech-bubble icon; Yes/No are blue capsule chat-bubble shapes identical to message bubbles — no press states, no system material. "Type an answer…" field is PURE BLACK (#000) on ~#1b1e26 card — hand-rolled material mismatch; plain gray circular up-arrow, not an SF Symbol. **R9:** keyboard fully open AND card AND composer (red stop) all simultaneously visible — stack consumes nearly all vertical space. Tool row above card restates the card's question (redundant competing representations). | R7, R9 |
| IMG_2535 | 2:19 | Card gone, `✓ clarify` settled row, no answer | Ghost text recurs (N1). Per-item thinking timer advanced 3s→18s while still an expanded row (N6, R5 evidence). ~320px dead band between thinking block and `✓ clarify >` row (R6). Card GONE though no answer bubble exists — premature dismissal/collapse pre-answer (N3, adjacent R8). | R5, R6, R8, NEW |
| IMG_2536 | 2:20 | 4-option / ≥30-char clarify prompt | Ghost text worst case: THREE lines of prior prompt bleed above "New chat", overlapping the gear/pencil/overflow icon row (N1). Thinking truncated `…at l… 2s ⌄` duplicated in full below (N4). ~450px dead band before `⟳ Asking Which directio…apore X trend monito… >` + cursor (R6). Keyboard open, no card. | R5, R6, NEW |
| IMG_2537 | 2:20 | 4-option card rendered | **R10 layout-clipping variant:** question text starts at very top and COLLIDES with the nav bar — "New chat" printed over it, `Singap[obscured]e` unreadable; question-mark icon at y≈257, under the nav bar. Card content pushed past safe area instead of scrolling in its own bounds. The 4 single-line capsule options DO fit/wrap here. Black "Type an answer…" field recurs (R7). Keyboard+card+composer stacked (R9). | R10, R7, R9 |
| IMG_2538 | 2:21 | Settled 27s turn + new 100-char prompt | Settled `Worked for 27s >` correct. Ghost bleed of prior reply (N1). New turn: thinking `…four options, w… 1s ⌄` duplicated (N4), then ~700px dead band (≈960px native) — the LARGEST gap observed (R6) — before `✓ tool.generating >`: green checkmark (completed) contradicting an in-progress raw state name (N2). | R6, R5, NEW |
| IMG_2539 | 2:21 | 100-char card rendered | **Most severe R10:** all 4 option bubbles HARD-CLIPPED at the right screen edge — no wrapping, no ellipsis, no horizontal scroll; text truncated mid-word, e.g. `Set up an automated daily morning sweep that scans` cuts off exactly at x=921 displayed / x=1260 native; rest of the ≥100-char option invisible and inaccessible. Unambiguously proves R10. Ghost bleed recurs (N1); ~450px dead band (R6); keyboard NOT open here (red stop present) — keyboard dismissal is timing-dependent, not an invariant (refines R9). | R10, R6, NEW |
| IMG_2540 | 2:21 | 100-char turn +1 step, card gone | Per-tool timer 1s→17s on an expanded row (N6/R5). `✓ tool.generating` resolved to friendly `✓ clarify` (confirms N2 transient ≈1s raw-name flash). ~700px dead band persists (R6). Card absent again with no answer bubble — premature disappearance reproduced on a second independent clarify turn (N3 not a one-off). Ghost bleed persists. | R5, R6, R8, NEW |

## Per-image findings (batch 2: IMG_2541–IMG_2549)

| Image | Time | Screen | Key findings | Bugs |
|---|---|---|---|---|
| IMG_2541 | 2:21 | Settled 30-char turn + live 100-char turn, stuck red stop | Settled `Worked for 27s >` correct. Live turn renders thinking TWICE: collapsed summary row `I need to create a clarification card with four options,… 20s ⌄` then full un-collapsed italic body beneath — collapse contract violated for the live turn while the settled turn above collapses correctly (N4/R5). Then `clarify ✓` settled row, `Got it`, then persistent inline orange `Not connected to the Hermes gateway` banner (not a toast — displaces layout). Composer still shows RED STOP while banner says not connected — contradictory turn-state signals in one frame. | R5, R6, R7, R11, C3 |
| IMG_2542 | 2:22 | Task-list turn starts | Live stack: spinning `Tasks 0/0` dock header, ~560px empty gap, then a tiny orphaned solid-blue vertical rectangle alone in empty space — a disconnected streaming-cursor artifact, not an inline cursor in a text bubble (R4 broken working affordance). Below: settled task pill `0 of 10 · Dummy task one — test item for UI rendering`. Blue focus ring + red stop + keyboard up. | R4, R6, R12 |
| IMG_2543 | 2:22 | Task reply settled + prompt re-sent, dock still 0/0 | First `Done — 8s ⌄` settled correctly (settled preview-then-full-text pattern fine). Re-send: full reply `Done — 10 dummy tasks loaded. Task #1 is marked in_progress…` sits directly above a `Tasks 0/0` dock header STILL spinning/0/0 though the streamed text says 10 tasks loaded and #1 in_progress — dock badge does not reflect the already-streamed tool result (R12/R13). Task pill repeats stale `0 of 10`. | R12, R13 |
| IMG_2544 | 2:22 | Task sheet expanded | All 10 rows present, each with a hollow RADIO-button-style ring (#1 has filled blue ring) — single-select radio group is a semantic mismatch for a todo list needing per-row pending/in-progress/done/cancelled affordances. Header still `0 of 10` despite 10 visible items (stale count). Plain dark rounded rect + thin divider — flat, not Liquid Glass/native list style (C1). | R12, R13, C1 |
| IMG_2545 | 2:22 | Task turn live, mid-tool | THREE stacked rows instead of one collapsed line: spinning `Tasks 0/0`, `tool.generating` (raw internal name leaking, N2), second `Tasks 0/0` — literal R5 defect reproduced on a task-tool turn. ~430px empty band between the settled `Done … 30s ⌄` summary and this stack (R6). Composer: red stop, idle model picker. | R5, R6 |
| IMG_2546 | 2:23 | Same turn +1m20s, 4th row appended | Same 3 rows + new `review.summary` row — each step appended as its own permanent stacked row instead of folding behind one `Working… ‹timer›` line; raw unhumanized names (C1 violation). Top summary timer `1m 20s` confirms a single long turn whose intermediate steps are all left simultaneously visible. | R5, R6, C1 |
| IMG_2547 | 2:25 | Same turn 2m39s, banner injected | Stack truncated to `Tasks 0/0` / `tool.generating` / `Tasks 0/0` / partial `Done — 10 dummy tasks…`, then orange `Not connected to the Hermes gateway` banner injected mid-transcript between tool-stack and task pill; pill still `0 of 10`; RED STOP still live. R11 exactly: live in-progress turn simultaneously reporting gateway disconnected; raw error shown as first-class transcript content (C3 violation). | R5, R6, R11, C3 |
| IMG_2548 | 2:25 | Banner + composer with 'Hi' typed | `Not connected` banner repeated a SECOND time (inline-in-transcript per 2547 + standalone line above composer — duplicate messaging, C3). Composer shows `Hi` but trailing action button is NOT the send arrow — renders as a bullet-list/task-list glyph (N5: wrong icon-state mapping or z-order collision). Task pill still `0 of 10`. | R11, R12, NEW (N5) |
| IMG_2549 | 2:25 | Same turn 3m9s, banner cleared | Timer `3m 9s`; same 4 rows (`Tasks 0/0` / `tool.generating` / `Tasks 0/0` / `review.summary`) with no rendered forward progress for 3+ minutes. No banner visible in this frame (self-healed) — banner appearance is transient/timing-dependent, but when shown it displaces layout and persists as stacked blocks, contradicting C3. Task dock stuck throughout. | R5, R6, R12, C3 |

*(Batch 2 forensics record was truncated mid-IMG_2549 in source; remaining frames IMG_2550–IMG_2576 pending.)*

## Machine-readable record (verbatim)

### Batch 1 (IMG_2529–IMG_2540)

```json
{"findings":[{"image":"/Users/abbhinnav/Downloads/IMG_2529.PNG","time":"2:13","screen":"Sidebar drawer — Sessions/Chats list (175 chats), search bar, Inbox/Automation rows, chat row list","observations":"The sidebar/drawer does NOT cover the full screen width: opaque session-list content occupies only the left ~75% (0-690px of 921 displayed / 0-945px of 1260 native); the remaining right ~25% strip (690-921px displayed) shows a SECOND, dimmer layer bleeding through simultaneously — a duplicate hamburger-menu circle, two clusters of skeleton shimmer loading bars, faint 'Aheli · /Us[ers]...' path text, and a partial composer ('Message', blue + button, 'qwe' text fragment) belonging to the destination chat view underneath. The floating blue 'New chat' pill (bottom-center, ~y=1158 displayed) straddles both layers, sitting half over the drawer and half over the leaked destination-view sliver. This is a compositing/coverage bug in the drawer presentation — it is not a clean full-cover slide-over; the chat view underneath (itself stuck on a loading skeleton) is visible through/beside it before any dismiss gesture. Matches R2 (drawer 'stuck'/imperfect presentation). Battery icon is critically low (red sliver) — worth noting for device-state context, not a UI bug.","bugRefs":["R2"]},{"image":"/Users/abbhinnav/Downloads/IMG_2530.PNG","time":"2:13","screen":"Chat/session view — pure loading-skeleton state (post drawer-select, pre content paint)","observations":"Full-screen shimmer-placeholder skeleton: several gray rounded bars of varying widths simulating message bubbles (both left- and right-aligned), no real content, no header title text (row under nav bar is blank), footer shows the real path breadcrumb 'Aheli · /Users/abbhinnav/Documents/02-Businesses/Aheli' already resolved even though the transcript above it has not painted. Composer at bottom is fully interactive/rendered ('Message Hermes...', model picker 'qwen3.8-max-preview', mic) while the transcript body above is still skeleton — i.e. the chrome (composer, breadcrumb) resolves before the transcript content, producing a visibly broken half-loaded state for a mid-price delay. Directly matches R3 ('session load still not clean, skeleton hangs, delayed paint').","bugRefs":["R3"]},{"image":"/Users/abbhinnav/Downloads/IMG_2531.PNG","time":"2:15","screen":"New chat session, settled turn ('Hi' → 'Hey. What can I help with?') plus a second in-flight user bubble 'How're you doing?', composer focused with keyboard open","observations":"Nav bar title collision: faint ghost text 'Hey Abhi. What's up?' is visible bleeding through/behind the 'New chat' title bar at the very top edge (y≈150-170 displayed, above and overlapping the hamburger/pencil/overflow icon row) — looks like stale/prior message content rendering un-clipped underneath the translucent nav bar for this frame. Settled-turn summary line reads correctly as a single 'Worked >' row (no stacking) — this settled state is fine per R5's requirement. Token/context footer '52568 tokens · ctx 52.5K' shown under the reply, unremarkable. Keyboard is up with composer focused (blue outline) for a NOT-yet-sent draft — normal, no clarify card present so R9 doesn't apply here.","bugRefs":["NEW"]},{"image":"/Users/abbhinnav/Downloads/IMG_2532.PNG","time":"2:17","screen":"Live turn in progress — long assistant reply already streamed, user reply 'I want Singapore specifically', then live turn state below it (Worked label + tool row + cursor)","observations":"Same nav-bar ghost-text bug: two lines of a previous turn's tail ('...ATEEZ tour)' and 'Toonami, #sundayvibes') bleed through above/behind the 'New chat' title. Live-turn stacking: 'Worked >' label, then a separate row '⟳ Reading https://getdaytrends.com/singapore/ >' (tool call), then a lone blue rectangular streaming-cursor block on its own line further down — three visually distinct stacked rows for what should be one collapsed 'Working…' line per R5. Dead vertical whitespace: ~70px gap between the user bubble and 'Worked', and a further ~70-90px gap between the tool row and the cursor block — small-scale instance of the R6 whitespace-gap pattern. Composer correctly shows the RED STOP button (square) during this live turn — stop affordance is present here (contrast with R4's report of the stop button sometimes missing).","bugRefs":["R5","R6","NEW"]},{"image":"/Users/abbhinnav/Downloads/IMG_2533.PNG","time":"2:19","screen":"Live turn — clarify-card being generated (thinking block expanded + tool 'Asking' row), no card yet visible, keyboard open","observations":"This IS the '02:19 photo' referenced by the spec for R6. Nav-bar ghost text again: 'Invoke a clarification card as...a yes or no question.' bleeds through/behind the 'New chat' title at the very top, overlapping the pencil/overflow icons. Below the title: an italicized thinking row 'I need to create a clarification card that poses a yes/no...' with a per-item timer '3s ⌄' next to it (expanded, not collapsed — R5 violation: this is a per-tool-call timer, not the required single per-turn timer), followed immediately by the SAME sentence repeated in full below it (visual duplication of the same thinking text, once truncated/inline and once expanded — redundant double-print of identical content). Then a LARGE empty band: roughly 320px of dead vertical space (displayed coords, ~440px native) with nothing in it, before the '⟳ Asking Should I set up...monitor that only pi... >' tool-call row appears, followed by the cursor block. This empty band is the clearest R6 evidence: an apparently reserved/oversized container between the thinking block and the tool-call row. Keyboard is open with no clarify card yet rendered.","bugRefs":["R5","R6","NEW"]},{"image":"/Users/abbhinnav/Downloads/IMG_2534.PNG","time":"2:19","screen":"Clarify card now rendered (Yes/No, Singapore trend-monitor question) stacked above the composer, keyboard fully open underneath everything","observations":"R7 (non-native card): the card is a plain flat dark-gray rounded rectangle with a small blue speech-bubble/question icon; 'Yes' and 'No' are rendered as blue capsule chat-bubble shapes (visually identical to the app's own message bubbles) rather than distinct native buttons — no button press states, no system material. Below the two options, a 'Type an answer...' text field is PURE BLACK (#000) against the card's dark-gray (~#1b1e26) background — a visible color/material mismatch, looks hand-rolled rather than using the card's own surface color; paired with a plain gray circular up-arrow send icon (not a native SF Symbol send affordance). R9 (keyboard should dismiss): the software keyboard is fully open AND the clarify card AND the composer ('Message Hermes...', still showing the red stop button) are ALL simultaneously visible on screen — composer + card + keyboard stack and consume nearly the entire vertical space below the transcript, exactly the violation R9 describes. Redundancy: the tool-summary row above the card ('⟳ Asking Should I set up...monitor that only pi... >') restates the same question the card itself displays in full — duplicate/competing representations of one question.","bugRefs":["R7","R9"]},{"image":"/Users/abbhinnav/Downloads/IMG_2535.PNG","time":"2:19","screen":"Same clarify turn, card has disappeared, thinking timer now '18s', tool row shows '✓ clarify' (green check) completed state, before any user answer was given","observations":"Nav-bar ghost-text bug recurs (same 'Invoke a clarification card as...a yes or no question.' text bleeding above 'New chat'). The per-item thinking timer incremented from 3s (2533) to 18s (2535) while sitting in an expanded, uncollapsed row — reinforces R5's note that 'the per-item timers visible per row are wrong; the timer is per-turn', since this timer is clearly counting a single sub-step, not the whole turn. Between the thinking block (ends ~y=430 displayed) and the '✓ clarify >' completed-tool row (starts ~y=751 displayed) is another large ~320px dead band — same R6 whitespace pattern reproduced. Notably the interactive Yes/No card from the previous frame is now GONE even though no answer bubble appears in the transcript — the card appears to have been prematurely dismissed/collapsed into the settled 'clarify ✓' row before an answer was actually given, which is a card-lifecycle ordering problem adjacent to R8 (post-answer collapse happening pre-answer) — flagging as related-to-R8/NEW since the transcript shows no user response yet the interactive affordance already vanished.","bugRefs":["R5","R6","R8","NEW"]},{"image":"/Users/abbhinnav/Downloads/IMG_2536.PNG","time":"2:20","screen":"New prompt: owner explicitly requests a clarify card with 4 options, each ≥30 characters (deliberate R10 stress test) — thinking block + tool 'Asking' row","observations":"Nav-bar ghost-text bug at its worst here: THREE lines of the prior prompt text ('Invoke a clarification card with 4 options to choose from a[nd each] / option is at least 30 characters') bleed through above/behind 'New chat', now tall enough to overlap the gear/pencil/overflow icon row itself, not just the title. Thinking block shows 'Abhi wants a clarification card with 4 options, each at l...' truncated with '2s ⌄' timer, duplicated in full below (same double-print pattern as 2533). Large dead-space band (~450px displayed) precedes '⟳ Asking Which directio...apore X trend monito... >' tool row and cursor block — R6 again. Keyboard open, no card yet.","bugRefs":["R5","R6","NEW"]},{"image":"/Users/abbhinnav/Downloads/IMG_2537.PNG","time":"2:20","screen":"Clarify card rendered for the 4-option/≥30-char test: 'Which direction do you want to take the Singapore X trend monitoring?' with 4 stacked blue-bubble options","observations":"Severe R10 evidence, layout-clipping variant: the clarify question text itself is rendered starting almost at the very top of the screen and COLLIDES with the nav bar — 'New chat' title sits printed directly on top of the question text, making the word 'Singapore' unreadable where the title overlaps it ('the Singap[obscured]e'); the question's leading question-mark icon sits at y≈257 displayed, effectively under the nav bar. This means the card content is pushing content up past the safe area / nav bar boundary rather than scrolling within its own bounds — a distinct manifestation of R10's 'overflows/clips the card'. The 4 options are each single-line blue capsule/bubble rows and do fit within the screen width here (text wraps to a 4th visible line 'No, not interested in trend monitoring at all' without clipping) — but the CARD HEADER TEXT clipping upward into the nav bar is the standout defect. 'Type an answer...' field again pure black against the dark-gray card (R7 material mismatch, recurring). Keyboard fully open together with card + composer (R9, recurring).","bugRefs":["R10","R7","R9"]},{"image":"/Users/abbhinnav/Downloads/IMG_2538.PNG","time":"2:21","screen":"Settled prior turn ('Worked for 27s', 'Noted — manual checks it is...') followed by new user prompt requesting a clarify card with 4 options each ≥100 characters (harder R10 stress test), then live thinking block + 'tool.generating' row","observations":"Settled-turn line correctly shows aggregated per-turn duration 'Worked for 27s >' (single row, matches R5's settled-state requirement — good). Nav-bar ghost bleed recurs, showing fragments of the PRIOR settled reply ('it — no monit[or]...esting the ca[rd]') overlapping 'New chat'. New live turn: thinking row '...four options, w... 1s ⌄' duplicated in full below, then a VERY large dead-space band — measured at roughly 700px in the 921-wide display (≈960px in the 1260-wide original), the largest whitespace gap observed in this slice — before a tool-status row reading '✓ tool.generating >' with a green checkmark. Mismatched status semantics: a green checkmark normally signals a COMPLETED action, but the label 'tool.generating' reads as an in-progress/raw internal state name leaking to the UI — the icon and the (unlocalized/unfriendly) label contradict each other. This is a distinct polish defect not explicitly in the ledger — likely a fallback label shown before the tool resolves to its friendly name (compare to 'clarify' label seen in 2535/2540 once resolved).","bugRefs":["R6","R5","NEW"]},{"image":"/Users/abbhinnav/Downloads/IMG_2539.PNG","time":"2:21","screen":"Clarify card rendered for the 4-option/≥100-char test — 'How do you want to handle Singapore X trend monitoring going forward?' with 4 option rows","observations":"Most severe R10 evidence in the whole slice: all 4 option bubbles have their text HARD-CLIPPED at the right edge of the screen with NO wrapping, NO ellipsis, and NO horizontal scroll — text simply runs off-screen and is truncated mid-word/mid-sentence, e.g. 'Set up an automated daily morning sweep that scans' cuts off exactly at the screen edge (x=921 displayed / x=1260 native) with the rest of the ≥100-character option invisible and inaccessible; likewise 'Build a high-signal monitor that stays quiet most of the...' , 'Skip automation entirely and keep things manual, so...', 'Drop trend monitoring completely for now and refocu[s...]' are all identically clipped. This directly and unambiguously proves R10: long option text is unreadable and unrecoverable in the current card. Nav-bar ghost-bleed recurs (3 lines of the previous 100-char prompt visible above 'New chat', now also overlapping the icon row). Large dead-space band (~450px displayed) again between the thinking block ('9s ⌄' timer) and the 'Asking' tool row — R6. Keyboard is not open in this frame (composer visible without keyboard, red stop button present) — contrast with 2534/2537 where keyboard was up simultaneously with the card, suggesting keyboard dismissal is inconsistent/timing-dependent rather than a hard invariant.","bugRefs":["R10","R6","NEW"]},{"image":"/Users/abbhinnav/Downloads/IMG_2540.PNG","time":"2:21","screen":"Same 100-char clarify turn one step later — card gone, thinking timer now '17s', tool row resolved to '✓ clarify >' (green check)","observations":"Same scene as 2538 but progressed: the per-tool timer advanced from '1s' to '17s' while still shown as an expanded per-item row (not per-turn) — further R5 evidence. The '✓ tool.generating' label seen in 2538 has now resolved to the friendlier '✓ clarify' label, confirming 'tool.generating' was a transient raw-internal-name flash visible to the user for roughly a second (minor polish bug, distinct from R5's collapse requirement). The large dead-space band between the thinking block and the tool-completion row remains (~700px displayed) — R6 reproduced identically to 2538. Nav-bar ghost bleed persists unchanged from 2538. As in 2535, the clarify card itself is once again absent from view even though no answer bubble exists yet in the transcript — same premature-card-disappearance pattern noted at 2535, now confirmed reproducible on a second, independent clarify turn (4-option/100-char one), not a one-off.","bugRefs":["R5","R6","R8","NEW"]}]}
```

### Batch 2 (IMG_2541–IMG_2549, source record truncated mid-2549)

```json
{"findings":[{"image":"IMG_2541.PNG","time":"2:21","screen":"New chat — settled clarify-card turn (30-char option variant) + live second clarify-card turn (100-char variant) still in flight, composer stuck showing red stop","observations":"Top: collapsed settled-turn line 'Worked for 27s >' (single-line collapsed format is correct here — this is the SETTLED-turn case working as intended). Below it the reply 'Noted — manual checks it is...' then a second user bubble re-invoking the clarify card with the 100-char-option variant. For THIS live turn the thinking preview is rendered TWICE: once as the collapsed summary row 'I need to create a clarification card with four options,... 20s ⌄' and then immediately again, in full, as un-collapsed italic grey body text directly underneath ('I need to create a clarification card with four options, where each option is at least 100 characters long...'). That is duplicate/uncollapsed content stacked directly below its own collapsed summary — the collapse contract is violated for the live turn even though the SAME transcript shows it working correctly for the settled turn above. Below that: a green-checkmark 'clarify ✓' settled row (chevron, tap-to-expand — looks reasonably native) then plain text 'Got it' then immediately an orange-warning banner 'Not connected to the Hermes gateway' sitting inline in the transcript flow (not a transient toast — it persists as a stacked block). Composer at the very bottom still shows the RED STOP/circle button (implying an active/streaming turn) while the banner one row above says not connected — directly contradictory turn-state signals visible in the same screenshot.","bugRefs":["R5","R6","R7","R11","C3"]},{"image":"IMG_2542.PNG","time":"2:22","screen":"Dummy-task-list live turn just started, keyboard open, composer focused with red stop button","observations":"Live-turn stack shows only a spinning 'Tasks 0/0' row (loading dock header) then a large empty gap (~560px) then a tiny, orphaned solid-blue vertical rectangle sitting alone in empty space with nothing else around it — this reads as a broken/disconnected streaming-cursor artifact rather than an inline cursor inside a text bubble (no text has streamed yet, no stop-button-adjacent affordance beyond the composer's own red button). Below the big gap: the settled outbox/task pill '0 of 10 · Dummy task one — test item for UI rendering' with a chevron. Composer has blue focus ring, red stop circle at trailing edge, keyboard fully up. The isolated blue bar strongly resembles the 'broken working affordance' called out in R4 (cursor bubble not tied to a real streaming container) and the large dead vertical band between the tasks header and the pill matches R6 exactly.","bugRefs":["R4","R6","R12"]},{"image":"IMG_2543.PNG","time":"2:22","screen":"Task-list turn settled reply + second identical task-list request re-sent, dock still 0/0","observations":"First exchange fully settled: 'Done — 8s ⌄' collapsed summary (correct single-line collapsed format for a finished turn) with full reply text below it (that's normal, not the live-turn duplication seen in 2541 — this is the settled preview-then-full-text pattern, which is fine). User then re-sends the same 'Start a task list with 10 tasks...' prompt. Second reply fully rendered ('Done — 10 dummy tasks loaded. Task #1 is marked in_progress...') sitting directly above a 'Tasks 0/0' dock header that is STILL spinning/at 0/0 even though the just-rendered text explicitly says 10 tasks were loaded and task #1 is in_progress — the dock badge count is not reflecting the tool result that already streamed. Below that, the settled task pill '0 of 10 · Dummy task one...' repeats the same wrong/stale '0 of 10' even though text says 10 tasks exist.","bugRefs":["R12","R13"]},{"image":"IMG_2544.PNG","time":"2:22","screen":"Task list sheet expanded (tapped the task pill)","observations":"Expanded task-list sheet shows all 10 'Dummy task N — test item for UI rendering' rows, each with a plain circular RADIO-button-style control (single hollow ring; task #1 has a filled blue ring/dot). Using radio-style single-select circles for a task/todo list is a semantic mismatch — todo items need per-row status affordances (pending/in-progress/done/cancelled), not a single-select radio group, which visually implies only one item can ever be 'selected'. The sheet header directly above still reads '0 of 10' (same stale-count bug as 2543) despite the sheet's own content clearly showing 10 items with #1 in_progress. Card chrome is a plain dark rounded rectangle with a thin divider — flat, not Liquid Glass / native list style (no native disclosure or system list separators/insets).","bugRefs":["R12","R13","C1"]},{"image":"IMG_2545.PNG","time":"2:22","screen":"Live turn re-running the same task-list prompt — mid-turn tool activity","observations":"Live-turn stack now shows THREE separate stacked rows instead of one collapsed 'Working…' line: a spinning 'Tasks 0/0' header row, a 'tool.generating' row (raw internal state-name leaking into the UI verbatim — not humanized), and a second 'Tasks 0/0' row directly below it. This is the literal R5 defect (Worked/toolCall stacked as separate rows) reproduced with a task-tool turn instead of a clarify turn — three rows where the ratified contract calls for exactly one. A large (~430px) empty vertical band sits between the collapsed 'Done — ...30s ⌄' summary near the top and this three-row tool stack, matching R6's whitespace-gap complaint. Composer unchanged (red stop, idle model picker 'qwen3.8-max-preview').","bugRefs":["R5","R6"]},{"image":"IMG_2546.PNG","time":"2:23","screen":"Same live turn ~1m20s later, one more row appended to the uncollapsed stack","observations":"Same three rows as 2545 ('Tasks 0/0', 'tool.generating', 'Tasks 0/0') plus a new fourth row 'review.summary' — confirming each tool/step of a single live turn is being appended as its OWN permanent stacked row rather than being folded into one collapsed 'Working… ‹timer›' line with an expandable bifurcation as required. The per-row internal state names ('tool.generating', 'review.summary') are raw/unhumanized strings, not native-looking labels — also a C1 violation. Timer next to the top collapsed summary now reads '1m 20s' confirming this is a single long-running turn whose intermediate steps are all being left visible simultaneously instead of collapsing behind one line.","bugRefs":["R5","R6","C1"]},{"image":"IMG_2547.PNG","time":"2:25","screen":"Same live turn now 2m39s in; 'Not connected' banner appears mid-turn; task pill still 0/10 with red stop","observations":"Turn has now run 2m39s. The live-turn stack is truncated to 'Tasks 0/0' / 'tool.generating' / 'Tasks 0/0' / partial 'Done — 10 dummy tasks loaded...' row, then immediately an orange 'Not connected to the Hermes gateway' banner is injected directly into the transcript (not a toast — it displaces layout and sits between the tool-stack and the task pill), and the task pill below it still reads '0 of 10' with the composer still showing the RED STOP button. This is R11 exactly: a live, in-progress turn (stop button live) is simultaneously reporting the gateway as disconnected — the turn-control layer and connectivity layer disagree, and the raw 'Not connected to the Hermes gateway' error text is shown as first-class transcript content rather than being suppressed per C3.","bugRefs":["R5","R6","R11","C3"]},{"image":"IMG_2548.PNG","time":"2:25","screen":"Same episode, composer now focused with keyboard up, user has typed 'Hi' while disconnected","observations":"Above the composer: 'Not connected to the Hermes gateway' banner repeated a second time (once inline in the transcript per 2547, once again as a standalone status line directly above the composer — duplicated messaging for the same condition). Composer shows typed text 'Hi' but the trailing action button (bottom-right, ~x1141,y1552 in original px) is NOT the expected send-arrow — it renders as a plain white/grey circle containing a bullet-list-with-dots glyph, the same glyph family used for the task-list pill icon elsewhere in the app. The send affordance appears to have been swapped for a task-list icon while offline+text-present, which is either a wrong icon-state mapping or a z-order collision between the send button and a task-list control. Task pill still shows '0 of 10' underneath, confirming the stuck dock persists across this composer interaction.","bugRefs":["R11","R12","NEW"]},{"image":"IMG_2549.PNG","time":"2:25","screen":"Same live turn now 3m9s in, connectivity banner cleared from view but composer unchanged","observations":"Turn duration now '3m 9s' on the collapsed summary — task-tool turn has been running well over 3 minutes with no forward progress in what's rendered (same 4 rows: Tasks 0/0 / tool.generating / Tasks 0/0 / review.summary). No 'Not connected' banner visible in this frame (self-healed) — banner appearance is transient/timing-dependent, but when shown it displaces layout and persists as stacked blocks, contradicting C3. Task dock stuck throughout.","bugRefs":["R5","R6","R12","C3"]}]}
```
